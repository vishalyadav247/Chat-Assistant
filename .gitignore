node_modules

# macOS
.DS_Store

# Build output / caches
/.cache
/build
/app/build
/public/build/
/public/_dev
/app/public/build
/extensions/*/dist

# Local dev databases
/prisma/dev.sqlite
/prisma/dev.sqlite-journal
database.sqlite

# Secrets — .env.example is the only one that ships
.env
.env.*
!.env.example

# Shopify CLI files created during app dev
.shopify/
.shopify.lock

# Auto-generated by react router
.react-router/

# Claude Code tooling.
# TRACKED — how and why this app was built, and what still needs verifying:
# CLAUDE.md, PROGRESS.md, specs/, skills/, agents/, qa/*.md,
# imp-details/ (the plan matrix the billing code reconciles against),
# review-checklist-run.md.
.claude/settings.local.json
# Design mockups and the python demo — all shipped, superseded by the app.
# Seed data moved to prisma/seed-data/ (the dev server needs it).
.claude/resources/
# Generated: npx tsx scripts/qa/make-test-matrix.ts
.claude/qa/test-matrix.xlsx
