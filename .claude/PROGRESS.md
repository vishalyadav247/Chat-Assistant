# ChatConvert — Project Progress

> The single tracking doc for spec-driven development. Update on every feature status change.
> Workflow: `.claude/skills/spec-workflow/SKILL.md` · Specs: `.claude/specs/` · Overview: `.claude/specs/00-overview.md`

## Current phase

**BUILD RUN COMPLETE** (2026-08-06). All 17 features done; all audits passed and findings fixed; all automated gates green. Remaining work = the PENDING-MANUAL dev-store session below, then feature-tier decisions + submission prep.

### Definition-of-done status (plan §gate)

1. Acceptance criteria recorded per feature (this table + spec deltas) ✅
2. Core suite green: typecheck 0 / lint / build / widget 9.69KB gz (≤30) / smoke / golden 9-case ✅ (2026-08-06 final run)
3. `prisma migrate status` clean — 4 migrations, all via guarded workflow ✅
4. Gate inventory: every matrix row → grep-able server enforcement point (15b report + `scripts/qa/APP-STORE-REVIEW.md`) ✅ — enforcement in ALLOW-ALL mode by user directive
5. Audits ✅ ALL COMPLETE: Waves 1/2 tenancy (0 leaks) + reviews (all fixed) · Wave-3 tenancy **0 LEAK** (R1 plan-escalation FIXED — verified-subscription-name derivation; N1 analytics cron scheduled) · Final Shopify review **0 Critical** — all 3 Major + 4 minor FIXED: M2 test-charge flag (BILLING_FORCE_TEST_CHARGES env for app review), M3 ctaUrl scheme validation (schema + client), m1 webhook backfills usage line item when callback never ran, m2 data-request redelivery dedupe, m3 embed-status behind EMBED_STATUS_ENABLED (skips doomed calls until read_themes), m4 campaign revenue recomputed server-side (client beacon ignored; test updated to assert ignoring)
6. Widget ≤30KB gz ✅; uninstalled shop renders nothing (code-verified; storefront confirm below)
7. E2E scripts all green: golden (chat paths incl. curated/recommendation/handover) · verify-compliance 36/36 (redact/export/purge/retention) · analytics fixtures · campaign metrics · billing mock 22/22 ✅
8. Pending-manual list ⬇️ ✅

### PENDING-MANUAL — the one dev-store session (walk in order)

1. ✅ DONE 2026-08-07 — installed on jgw-check.myshopify.com. Scopes now 6 (`write_app_proxy` added). **Start dev on a self-managed tunnel**, not a bare `npm run dev` — see the decisions log and README.
2. ✅ DONE — app embed enabled; widget renders on the storefront
3. ✅ DONE — **SSE streams correctly** (frames ~500ms apart through a Cloudflare quick tunnel). Fallback transport not needed. Details in the Phase 0 checklist below.
4. **Storefront walkthrough**: launcher variants, chat streaming, product cards + one-click ATC + cart drawer, pre-chat form, FAQ screen, order tracking, handover ("talk to a human") → reply from /app/inbox → widget delivery + Seen, survey, proactive campaign bubble on home page
5. **Admin walkthrough**: all 10 nav pages render; chatbox live preview parity; training tabs (run Sync products); Test AI console incl. Review source
6. **Billing test charges** (dev store, test:true): subscribe each tier monthly + one yearly; verify callback lands plan + trial; cancel/downgrade; check `app_subscriptions/update` webhook fires post-deploy
7. **Semantic re-verification**: `npx prisma db seed && npm run smoke && npx tsx scripts/eval-golden.ts` (already green — re-run to confirm on your machine)
8. **Lighthouse** on a storefront page with the widget enabled (no degradation >10 pts; widget is deferred + 9.69KB)
9. Before submission: privacy policy URL (must name OpenAI as processor), listing copy/screenshots, support contact — then re-run `scripts/qa/APP-STORE-REVIEW.md` items 8/9. **DONE — the listing has been live since 2026-07-22** at `https://apps.shopify.com/chatconvert-2`. That slug is now the last-resort default in `app/lib/review.ts` (`DEFAULT_APP_STORE_HANDLE`), so no env var is needed; Admin → Settings and `SHOPIFY_APP_STORE_HANDLE` still override it if the slug ever changes.
10. **When you define final plan tiers**: edit the matrix on `/admin/plans` (operator dashboard, spec 19) + flip its Enforcement switch — every gate activates, no code changes. (The code matrix in `app/lib/billing/plans.server.ts` is now only the DEFAULT the dashboard overrides.)
11. **Web app (spec 18)**: generate VAPID keys (`npx web-push generate-vapid-keys` → `VAPID_PUBLIC_KEY/PRIVATE_KEY/SUBJECT`), set `EMAIL_PROVIDER=resend` + `RESEND_API_KEY` + verified `EMAIL_FROM`; walk: Inbox → Open in web → invite an agent → accept in a private window → push on a storefront handover. App Store testing instructions must include a **web test login** (req 4.5); privacy policy must name Resend as a processor and mention team-member accounts + push endpoints.

Waves: 0 ✅ · 1 (02/03/16/05/06) ✅ · 2 (modals/09/04/07/08/10/11) ✅ · 3 (15/12/13/14/17) ✅

## Feature table

Status: `not-started` → `spec-ready` → `in-progress` → `review` → `done`

| # | Feature | Spec | Status | Depends on | Notes |
|---|---------|------|--------|-----------|-------|
| 00 | Overview & guidelines | `00-overview.md` | done | — | Living doc |
| 01 | Foundation (DB, jobs, proxy/SSE, skeleton) | `01-foundation.md` | done | — | All automated criteria pass; dev-store items in pending-manual |
| 02 | Shopify catalog sync | `02-shopify-sync.md` | done | 01 | Code complete: caps seam, collections/discounts sync+webhooks, daily reconcile cron, afterAuth initial sync + default config seeding. Master learn-toggle UI → 07. Dev-store verification pending-manual |
| 03 | Runtime AI pipeline | `03-ai-pipeline.md` | done | 01, 02 | **GOLDEN SET PASS (7/7 live w/ OpenAI)** 2026-08-06. prompts.ts FROZEN. Diamond-necklace case → off_topic accepted (persona scope, documented in eval). Multi-turn history continuity test → with 05 storefront testing |
| 04 | Knowledge ingestion | `04-knowledge-ingestion.md` | done | 01 | Built (subagent, 48/48 verification incl. live SSRF suite + real-embedding round-trips for manual/pages/csv/file(txt) + FAQ bridge). Jobs + weekly recrawl cron wired. Deltas: **PDF/DOCX parser deferred (dependency decision needed — sources error cleanly)**; shopPolicies scope worth a docs check before 07 wiring; suggested-Q&A generator behind flag |
| 05 | Storefront widget | `05-storefront-widget.md` | done | 01, 03 | Built (subagent + ATC/beacon upgrades): renderer module (06 reuses), transport w/ fallback seam, all screens, prechat, survey, human-poll, real one-click ATC via variantId, 7.09KB gz initial (≤30KB). Deltas in spec. Storefront render/SSE/Lighthouse pending-manual |
| 06 | Chatbox settings | `06-chatbox-settings.md` | done | 01, 05 | Built (subagent): 3 tabs + live preview using the STOREFRONT renderer via ?raw seam (parity by construction, all 7 criteria pass). Deltas: starter answers plain-text v1, import-from-FAQs deferred, up/down reorder, activate-while-dirty discards edits (edge), extra file renderer-assets.server.ts. Onboarding-step flip → 13 |
| 07 | AI Agent home + training data | `07-ai-agent-training.md` | done | 02, 04 | Built (subagent, 12/12 live FAQ checks, all 6 criteria pass). Unresolved-queue population wired into pipeline by orchestrator (dedupe-by-text, 3 fallback sites). Deltas: FAQ plain-text editor + up/down reorder; export/import partial round-trip; discounts real-time switch display-only; policies-only connector (Pages need read_content — deliberate); pdf/docx inherit 04 delta |
| 08 | AI instructions + recommendations + handover config + Test AI | `08-ai-instructions.md` | done | 03 | Built (subagent, criteria pass w/ live evidence: no-meter Test AI, review-source, curated>recommendation ranking). Orchestrator added the missing runtimes post-landing: custom-recommendation buy-lane constraint (term match → hand-picked pool) + cross-sell companion append (6-card cap); stale tooltips cleaned. Deltas: OOS/overstock toggles display-only (OOS hard-enforced in SQL); lazy-cache re-embeds replace the spec's "re-embed job" wording; collections constraint deferred (membership not mirrored) |
| 09 | Curated answers | `09-curated-answers.md` | done | 02, 03 | Built (subagent, all 6 criteria pass — CRUD/revalidation verified live vs dev DB). Browse modals shipped as shared components (08/12 reuse). Deltas: collection filter resolves membership live via Admin GraphQL; served-KPI all-time v1; "pending index" pill for unembedded answers. Daily revalidation job wired by orchestrator |
| 10 | Inbox + human handover | `10-inbox-handover.md` | done | 03, 05, 08 | Built (subagent, all 7 criteria scripted-pass incl. AI→handover→merchant-reply→widget chain, dormancy, leave-message lead, auto-resolve, seen receipts). Auto-resolve cron wired (*/10). Deltas: email notify = logged seam (provider decision pending); cart card renders pageContext.cart else empty state; composer textarea; shopper-facing blocked UI deferred |
| 11 | Contacts | `11-contacts.md` | done | 05, 10 | Built (subagent, 37/37 live checks incl. export RFC-4180/UTF-8 round-trips + negative cross-shop tests). Deltas: anonymous auto-creation not yet (tile reads 0); email→customer Admin-API matching deferred (session-id classification only); export = client-side Blob save (embedded-admin token constraint), async job deferred; inbox links not deep-linked |
| 12 | Proactive chat campaigns | `12-proactive-chat.md` | done | 05, 15 | **Rebuilt 2026-08-26 to `.claude/resources/proactive_chat/*.png`** — see the decisions log for the full change. Data-driven editor (Activate card + General/Trigger/Conditions/Message/Appearance + live preview & summary column) shared by all 10 templates; settings reshaped to `{trigger,conditions,message,appearance}` with a legacy migrator; per-campaign bubble colors; contextual recommendations + floater variants via `proxy.campaign-products`; newsletter leads via `proxy.campaign-lead`. Deltas: KPIs still all-time (range chip static — event aggregation w/ 14); **Product Quiz is a disabled `Pro +` tab only** (as in the reference — no quiz runtime); "best sellers"/"new arrivals" read the merchant's curated Recommendation lists (spec 08) since the catalog mirror has no order-volume column; priority via ▲▼; metric bumps updatedAt (frozen model). Storefront visual pass → pending-manual
| 13 | Dashboard + onboarding | `13-dashboard-onboarding.md` | done | 02, 06, 09, 12 | Built (subagent, all criteria script-verified live: checklist detection per step, metrics math 3 ranges + deltas + isTest exclusion, live feed tags). Deltas: assisted-revenue KPI = "Chat add-to-carts" proxy (orders/PCD deferred); **embed detection needs read_themes — NOT added (re-auth cost), returns "unknown" w/ verify-in-theme-editor link, code ready**; count-up/sparkline animations omitted (Polaris-native) |
| 14 | Analytics | `14-analytics.md` | done | 03, 10, 16 | Built (subagent, all 5 criteria via deterministic 3-day fixtures — exact-number assertions incl. weighted response averages, donut largest-remainder 100%, rollup idempotency, isTest exclusion everywhere). Rollup cron wired. Deltas recorded in spec by agent (event counters via Message.sourceLayer, human≈handover, 3m daily buckets, sync exports w/ 5000 cap) |
| 15 | Billing & plans | `15-billing-plans.md` | done | 01 | 15a metering (Wave 0) + 15b built (subagent, 22/22 mock-flow checks): BillingProvider real+mock, callback, app_subscriptions webhook (replaced-sub guard), overage usage records, full plan page w/ verbatim FAQ, gate inventory produced. ENFORCEMENT stays open. Deltas: discount code cosmetic (no API param in 2026-07); contact mailto constant; real test charges NEEDS-MANUAL (+ deploy for webhook) |
| 16 | Settings | `16-settings.md` | done | 01 | Built (subagent): all tabs/sub-views, hash deep links, saves + cache invalidation, IANA timezones, availability preview. Deltas recorded in spec. Embed detection→13, retention job→17, survey firing→05/10 |
| 17 | Compliance & GDPR | `17-compliance-gdpr.md` | done | 01, 11, 16 | Built (subagent, **36/36 E2E checks live**: export exact-scope, redact w/ untouched neighbors incl. cross-shop same-email, **zero-row purge count-assert across all 27 tables + sessions**, retention window). Data-request export = compute-on-download (no stored PII artifact — spec delta, documented). Executed review checklist: `scripts/qa/APP-STORE-REVIEW.md`. Reminder job wired via registerComplianceJobs. Email delivery = logged seam |
| 19 | Standalone web app + team logins + push | `18-web-app-team.md` | built — NEEDS-MANUAL dev-store pass | 10, 15, 16, 17 | Built 2026-08-19: dual-surface `requireShopAccess` seam (same /app URLs; admin = authenticate.admin, web = HttpOnly cookie → `TeamSession`), `TeamMember`/`TeamSession`/`PushSubscription` tables (roster JSON backfilled, migration `20260819180000_team_web_access`), web routes (`/web/login`, invite, forgot/reset, handoff, logout), ChatConvert web shell (no App Bridge; `useAppBridge` shim + web SaveBar), roles (agent = Inbox+Contacts), Settings→Team rebuilt (invite email/copy-link, resend, reset link, disable, remove, seats), Account page (password, push prefs), `Open in web` handoff, Web Push via `web-push` + `public/sw.js` + pg-boss `team-notify` job (handover / human-mode shopper reply / new-conversation opt-in; handover email), inbox SSE change feed on both surfaces, Plan & Usage read-only on web, cleanupShop + verify-compliance (30 tables) extended. Email provider = Resend behind `EMAIL_PROVIDER` (log fallback). Typecheck/lint/build/verify-compliance green; curl smoke of the auth seam (cookie/marker/bogus/handoff/login/logout/SSE/403 page) passed against the built server; tenancy-auditor 0 LEAK + shopify-reviewer 0 Critical, all Majors/minors fixed (see spec §Audit record). Follow-up 2026-08-19: web shell lost its CSS in dev — root cause was a pre-existing hydration mismatch (`<style>{CSS}</style>` text escaping) that made React re-render the document and drop Vite's dev styles; all inline stylesheets now use dangerouslySetInnerHTML (rule added to polaris-admin-ui skill); all date/time display now goes through the shop-wide formatter (`app/lib/format/datetime.ts`, `useDateTime()`; Settings → General → Date & time format + store time zone) — deterministic across SSR/client; web shell/auth CSS are plain stylesheets served through route `links` (survive any client re-render); owner bootstrap retries the Admin API email lookup when a placeholder `owner@<domain>` was stored. Follow-up 2026-08-19 (user report): invite button was disabled by the seat line (displayQuota vs open enforcement → gate now uses getQuota, unlimited while open); push was off because VAPID env was absent → VAPID keys now auto-provision into `app_secrets` (migration `20260819190000_app_secrets`), env overrides; added `EMAIL_PROVIDER=smtp` (nodemailer) next to Resend for invite/reset/handover emails (log + copy-link remains the no-config fallback). NEEDS-MANUAL: admin regression + web flow on the dev store, push on a real browser (click Enable notifications), email provider env. |
| 21 | Mobile responsiveness (all pages, both surfaces) | `20-mobile-responsive.md` | built — NEEDS-MANUAL phone pass | 19 | Built 2026-08-20: single 768px breakpoint (`BP_MOBILE`/`MOBILE_MEDIA` tokens, `useIsMobile()` hook, global `app-mobile.css` via app.tsx links — desktop >=769px pixel-identical, all rules media-gated). Web shell: sticky top bar (hamburger + Inbox unread pill) + slide-in drawer <=900px. Inbox: one pane at a time keyed off ?c= (list with horizontal filter chips <-> full-screen thread with back button), details reused as a right slide-over via a thread-header info button wherever the Details column is hidden (<1241px — restores the assignee select at tablet widths too), visualViewport-aware height fit (composer above the keyboard), 16px inputs (no iOS zoom). DataTable: x-scroll wrapper, desktop-only column widths, wrapping bulk bar + footer pager (fixes contacts/curated/review/proactive/browse-data). .cc-split stacks chatbox/curated/TestAiConsole/PlanCards. FAQ tree columns shrink-to-content on phones. Touch reordering: ReorderButtons (up/down) beside every drag handle on coarse pointers. Analytics chart pans (min 640px); contact panel + availability + contact-method rows wrap; SaveBar safe-area. Design pass v2 2026-08-20 (user: trendy/high-class mobile design, reviewed in browser): reviewed every page at 390px via Chrome (in-page viewport simulator — extension pins CDP metrics at 1920, resize_window corrupts the window; media-query rewrite + 390px column instead), then polished — 2-up compact KPI grid (.cc-statgrid/.cc-stattile), app-like drawer (icon chips per nav item — NAV gained icon names, mobile-drawer-only; 44px items, rounded edge, blur scrim, spring curve, safe-areas), top bar shows shop name, compact push notice, inbox v2 (filled accent filter chips, search+unread on one row, 44px avatars, 52px thread header, rounded composer + circular send, s-page heading dropped <=768 for full-height workspace). Verified in-browser: list/thread/drawer/details + dashboard 2-up tiles. Design v3 2026-08-20 (user supplied Chatty reference screenshots in .claude/resources/mobile_layout/ — layout only): drawer restyled LIGHT (white active card + accent icon chip; desktop rail stays dark), top bar gained member avatar chip (-> /app/account), inbox filter chips replaced by a left slide-over reusing InboxFilters (filter button + active-filter title + Unread toggle row, search below), KPI tiles back to stacked 1-col flat tiles, TabPills horizontally scrollable <=768, PlanCards = swipe snap-carousel with peeking card. Verified in-browser (list, filter panel, drawer, plans, dashboard). Spacing/type polish (user review): 14px page gutters on the web surface (.ccws-content — Polaris s-page is display:contents and provides none at phone widths, cards ran edge-to-edge), hero 16px padding + 18.5px title, StatTile values 25->21px with tighter tile gap <=768. Height/notification polish 2026-08-26 (user: chat window height, notifications): inbox composer is ONE row on phones (input + circular send inline, emoji strip hidden — the keyboard has its own; rows=1 via useIsMobile) and the push notice is a single 48px row with short copy — together ~110px back for the message list (measured 390x772: messages 390->469px, composer 130->68px); ResizeObserver re-pins the newest bubble after the JS height fit and on keyboard open. Chatbox preview no longer hijacks the page (storefront CSS takes .cw-panel position:fixed;inset:0 below 480px — overridden inside .ccpv, and on phones the preview starts collapsed to the launcher). Push enable path hardened: session-expiry detected (fetch follows the /web/login redirect, so a 200 HTML answer is NOT success), VAPID applicationServerKey mismatch re-subscribes, per-cause error copy, device count revalidates, Send-test button, and a web app manifest + icons so iOS (Home Screen install) can receive push at all. E2E agent sweep 2026-08-26 (user: login to reply on desktop/tablet/mobile, iOS+Android): ran the real flow as a throwaway agent account (created then deleted) at 1920, 834 and 390px - login, inbox, open thread, send reply (desktop AND phone layouts), back, filter slide-over, details slide-over + assignee change (persisted), drawer nav, account page. All passed. Fixes from the sweep: (a) iOS focus-zoom - Polaris fields render their input at 13px inside an OPEN shadow root no page CSS can reach (host font-size and ::part both fail), so app/lib/ui/ios-input-zoom.ts adopts a 16px sheet into each field's shadow root, gated on (pointer: coarse) with a MutationObserver for late upgrades (desktop verified pointer:fine, never runs); (b) 100vh gained a 100dvh companion on .ccws-shell/.ccws-rail/.ccwa-page (iOS large-viewport overshoot; the drawer already used dvh); (c) push-notice buttons white-space:nowrap (they wrapped at 769-900px); (d) 40px touch targets for thread-header controls + filter button under @media (pointer: coarse); (e) agents no longer see the dead 'Open Shopify admin' rail link (ChatConvert-only accounts); (f) inbox live feed wakes on visibilitychange/pageshow - a phone freezes a backgrounded tab and the SSE stream dies with it, so returning drops the stale stream, cuts the backoff short and revalidates once (verified: exactly 1 refetch, no reconnect storm). NOT verifiable without hardware: real iOS Safari / Android Chrome rendering, Home-Screen install + push delivery, on-screen-keyboard behaviour. Push-on-mobile triage 2026-08-26: DB shows 5 subscriptions, ALL Windows-Chrome desktop, none mobile, while an Android Chrome web session exists - so registration fails ON the device, not delivery (a real probe through sendPushToMembers returned sent:5/pruned:0, and there are zero push_* rows in app_logs). Tunnel assets verified reachable over the LIVE quick tunnel (/sw.js 200 text/javascript, /manifest.webmanifest, /icon-192.png) - note shopify.app.toml still carries a DEAD tunnel host, the live one is in .shopify/dev-bundle/manifest.json. Added device-level diagnosis instead of guessing: pushDiagnostics() (secure context, SW/Push/Notification APIs, permission, SW registered, subscription + endpoint host) rendered as a checklist on the Account page; PushResult now carries a raw `detail` (exception name/message, or POST status) shown as 'Last attempt - ...'; failures are ALSO reported to the server via a `{report}` body on /app/push-subscription -> logWarn('push_enable_failed') so they land in /admin/logs; the checklist re-reads after each attempt (it was a mount-time snapshot showing a stale cross); 'insecure' is now its own failure reason (http origin - service workers and Notification are absent outside https/localhost); Send test now shows whenever ANY device is registered. ROOT CAUSE of 'no push on mobile' (2026-08-26): the phone's Enable hit the plan gate ('available on the Basic plan and above') even though the shop is Plus - because applyConfig() REPLACED a plan's feature array from the stored admin:plans override: merged.features = GATED_FEATURES.filter(f => patch.features.includes(f)). The saved override was a snapshot from before survey / push_notifications / custom_recommendations / multi_language existed, so all four were silently gated OFF for Plus forever (quotas were unaffected - they merge per key). Fix: plan patches now carry knownFeatures (the gated list as it stood when saved; /admin/plans stamps GATED_FEATURES on every save), and the merge distinguishes on / off / didn't-exist-yet - unknown features fall back to the plan default. Legacy rows (no knownFeatures) therefore restore defaults for anything they never saw. Verified: Plus back to all 13 features incl. push_notifications, operator's edited quota (conversations 1200) preserved, active_campaigns falls back to unlimited; free=false, basic/pro/plus=true. Composer parity + focus fix 2026-08-26 (user: mobile chat input looks bad while typing and unlike the desktop bar): the phone composer went back to the DESKTOP shape - reply field, divider, emoji strip with Send at the right - just scaled (one starting row, 32px emoji, 34px send, 9/11 padding): 102px idle, 124px while typing, vs 130px on desktop and the 68px stripped pill it replaced. Focusing the field now undoes the browser's scroll-into-view after the keyboard settles (window.scrollTo(0,0) + re-pin the newest message, mobile only) - that jump was what cropped the thread header. Fixed a real paint bug found while measuring: `.cin-grid button{background:none;color:inherit}` (0,1,1) outranked `.cin-send` (0,1,0), so the Send button never got its gradient or white glyph - only the purple glow meant to sit under them - on BOTH surfaces. Base + mobile rules are now `button.cin-send`; verified computed background is the brand gradient with white icon. Desktop Send therefore changes appearance (to what the CSS always intended). Device screenshots triage 2026-08-26 (.claude/resources/other/mobile_chat_1..3.png, real Android Chrome at 360x~600 CSS): three defects the 390px simulator never showed. (a) Dead strip under the workspace (~57 CSS px): the shell's page gutter (.ccws-content padding-bottom 20px + safe-area) sat BELOW a workspace that already measures itself to the viewport bottom, so the JS fit's second pass subtracted it again - the padding is now dropped for the inbox via .ccws-content:has(.cin-grid) and the fit's 16px slack is 0 on phones (the composer carries its own safe-area margin). (b) Keyboard state was broken (screenshot 3: top bar scrolled away, workspace collapsed to a band mid-page, composer clipped): Android's default interactive-widget=resizes-visual shrinks and SCROLLS the visual viewport inside an unchanged layout viewport, so height = visualViewport.height - top left the grid tiny and mispositioned. Fixed with interactive-widget=resizes-content in the viewport meta (Android resizes the layout viewport instead) PLUS the fit now measures to visualViewport.height + offsetTop for browsers that ignore it (iOS). (c) .cin-msgs gained min-height:0 so the message list is what shrinks when space is tight - the composer can no longer be pushed out of the card. Remaining 16px above/below the workspace comes from s-page's own shadow-DOM padding (unreachable from page CSS) and reads as intentional card inset - left alone. NEEDS-MANUAL: phone walkthrough (user) — inbox list/thread/back/details/assign/reply, drawer nav, tables, editors, FAQ reorder. 2026-09-04 (filter rail redesign, user: "conversation filter button is not looking a feature"): the rail read as seven words on a white card, indistinguishable from the data columns beside it. Rebuilt as navigation — tinted gradient column, branded sticky header with a live conversation total, a coloured stroke glyph per tab (new FilterIcon + FILTER_ICON_PATHS in InboxShared, semantic tint per data-k), count chips as pills. Selected tab = frosted brand pill (translucent gradient + backdrop blur + 3px accent bar + white gradient glyph chip); the slide-over panel and its scrim are the only other glass surfaces. Deliberately NOT applied to conversation rows, bubbles or counts — those need contrast, not translucency. Trigger button got the same pill treatment + a brand dot whenever filter !== "all" (new `filtered` prop on InboxList), so a filtered list can't be mistaken for an empty one. BUG FIXED: .cin-filcol hid at ≤1040px but .cin-filbtn only appeared at ≤768px — 769-1040px (every tablet, and a narrow desktop window) had no way to reach the filters at all; the trigger now shows in the same breakpoint that removes the rail. Coarse pointers get 44px rows; prefers-reduced-motion drops the transitions and the panel slide-in.  Same-day follow-up (user): the gradient mark beside the rail title was noise — dropped; and the filter overlay was a left drawer, identical in shape and gesture to the web shell's nav drawer, so the two read as one control. The nav drawer is unchanged; the filter overlay is now a **bottom sheet** (`variant="sheet"` on InboxFilters — grab handle, titled header with a close X, tabs as a 2-column chip grid, rises from the bottom under the thumb, capped at min(78dvh,520px) so the list stays visible behind it) and above 768px the same panel centres as a 460px dialog instead of a stranded sheet.  Then, phones only (user: "filters in the bottom … take all the filters and unread toggle in the bottom … between the header and bottom menu all the conversation can be scroll"): the sheet is gone at ≤768px — no trigger, no overlay. `variant="bar"` renders the seven tabs PLUS the Unread toggle as one scrollable strip pinned to the bottom of the list pane (grid row 2; hidden in thread view, which needs the height for the composer). The list header keeps only the search — its title just restated the active chip. Inbox is now the one page that drops the shell's page gutter entirely (`app-mobile.css` padding 0) and `.cin-col` loses its border/radius/shadow, so list, thread and bar run edge to edge; the bar only reads as a bar when it touches both screen edges. 769-1040px still gets the sheet/dialog, desktop rail untouched.  Revised again after review (user: "search and bottom menu should be end to end no padding", "scrolling option is not good"): the scrollable chip strip became a **fixed five-slot tab bar** — Unread · All · Open · Handover · More — equal widths, flush to both screen edges, 56px slots with the glyph above a 10px label, the count as a superscript on the glyph, and a 2.5px brand bar on the top edge of the active slot. A drag-strip hid the state it existed to show and was a poor one-handed target. The three remaining filters (Resolved, Unassigned, Starred, Blocked) live in a small popover that opens upward from the More slot (transparent scrim + capture-phase Escape so it closes before the route's own overlay handlers); when one of them is selected the More slot adopts that filter's glyph, label, count and active styling, so the bar never shows All highlighted while the list shows something else. Search is now the header: full-bleed, 48px, no radius, 16px text (kills the iOS focus-zoom), its own bottom hairline as the divider.  Final pass: **`s-page`'s own gutter was the remaining inset** — it lives on a `.page` element inside the component's shadow root, which no page stylesheet can reach (s-page is display:contents). New `app/lib/ui/spage-fullbleed.ts` adopts a stylesheet into that open shadow root, same technique as installIosInputZoomFix; the `@media (max-width:768px)` sits INSIDE the adopted sheet so it follows rotation with no listener, and the inbox route is the only caller (cleaned up on unmount). Unread left the tab bar for the right of the search — it is a modifier that composes with whichever tab is selected, not an eighth filter — freeing its slot for Unassigned, so the bar is All · Open · Unassigned · Handover · More (overflow: Resolved, Starred, Blocked). Search is 46px with its own border and radius inside the flush row, and the focus ring is on the input alone: the earlier `.cin-list-top:focus-within` lit the whole row up whenever the Unread button took focus.  Thread view, same pass (user: "in the top no padding", "chat input should look like whatsapp … add a button to select the emoji"): header tightened to 48px/6px and `.cin-msgs` lost its top padding so the list starts under the header (the day divider already gives the break). The composer keeps ONE markup for both surfaces — on a pointer it is still the design's two-row chat bar; at ≤768px CSS collapses it to the messaging-app shape: a rounded white pill holding an emoji button and the field, a round 44px Send beside it, and `display:contents` on `.cin-comp-bar` promoting Send to a sibling of the pill so no branch or hydration flash is needed. The six-emoji strip left the flow for a popover above the pill (scrim-dismissed, refocuses the field after inserting). The old strip sat directly under the field, exactly where a thumb reaching for Send landed.  Composer round 2 (user): field starts at exactly one line — the `min-height` floor was inflating it, so the autosize effect now owns the height from the first paint up to the 3-row cap; pill and Send squared off to a 5px radius (was 22px/50%); emoji button up to 40px/23px; the picker went from the desktop strip's six to a 24-emoji, six-per-row grid spanning the composer width (`EMOJI_PICKER`, separate from the desktop `EMOJI` strip so that stays as designed). Reading sizes up on phones: bubbles 13→15px/1.5, composer 16→17px, meta 11→11.5px. Emoji-only messages now render bubble-less at 34px (`isEmojiOnly`, Extended_Pictographic + modifiers/RI/ZWJ/VS16 only — Emoji_Component would have matched "2025 🎉"); scoped to ≤768px to keep the desktop thread untouched. |
| 20 | Admin panel (operator dashboard) | `19-admin-panel.md` | done (audit noted below) | 15 | Built 2026-08-20: company-side `/admin` dashboard (non-embedded, DB admin accounts `platform_admins`+`platform_sessions`, migration `20260820054345_platform_admin`, bootstrap `scripts/admin-account.ts` or one-time env creds). Pages: overview (shop/plan aggregates — cross-tenant BY DESIGN, spec §exemption), AI model (chat model + optional temp/max-tokens overrides that win over per-call tuned values; embedding model read-only), Plans (full matrix editor + enforcement switch + reset-to-defaults), Admins. Overrides in `app_secrets` (`admin:ai`/`admin:plans`), merged into `PLANS` IN PLACE (30s TTL refresh, zero churn for 22 consumers). Verified: typecheck/build/smoke green; `scripts/admin-check.ts` 12/12 (override→gate→persist→reset round-trip); curl auth walk on built server (redirects, bogus cookie, wrong pw, login cookie Path=/admin, logout revocation). **Dev server restart needed** for the new Prisma client. Lint: 2 pre-existing errors in app.inbox.tsx belong to the in-flight mobile change, not this feature. Tenancy audit **0 LEAK**; its 4 RISK + 2 NOTE hardening items all fixed same day (remove-race transaction, per-account lockout `20260820061959_platform_admin_lockout` + lockout probe script, timing-safe bootstrap compare, sameOrigin on all authed actions, quota-key validation, Secure cookie in production) — see spec §Audit record |
| 18 | App Store review prompt | `APP_REVIEW.md` | done | 01 | Built 2026-08-14: `shopify.reviews.request()` on every admin app load via `ReviewPrompt` in app.tsx (once-per-mount ref guard + old-App-Bridge `reviews` API guard); gate = pure predicate in `app/lib/review.ts` (install age >24h AND ≥1 non-test conversation), resolved read-only in the shell loader (`review.server.ts`, no upsert, fails closed). **No prompt state stored** — Shopify enforces cooldown/annual cap/already-reviewed. Decisions: engagement = real shopper conversation; ask forever (Shopify's limits throttle). Fallback deep link (`#modal-show=WriteReviewModal`, target=_blank) in Settings→General renders nothing until the `SHOPIFY_APP_STORE_HANDLE` env var is set to the live listing handle (threaded via the settings loader). Modal only testable on a dev store (backdate `shops.installedAt`); returns `merchant-ineligible` everywhere until the listing is live |
| 22 | Logging & observability (operator log console) | `21-logging-observability.md` | done | 20 | Built 2026-08-21: `app_logs` table (migration `20260821090000_app_logs`) + write seam `app/lib/log.server.ts` + `/admin/logs`. **Errors and warnings only** — no info/debug level exists, so there is never a row per request; that plus a **14-day purge** (in the nightly `retentionPurge` job) is what keeps logs in Postgres instead of a paid log drain. Seam mirrors every call to console (survives a DB outage), never throws, never awaited (same contract as `llm/usage.server.ts` — it sits in the chat hot path), rate-caps at **50 rows/hour per event code** (51st writes one `log_rate_capped` row), denylist-redacts PII/credential context keys and caps context at 2 KB. Call sites that only hold a `*.myshopify.com` domain pass `shopDomain` and the seam resolves it to a shopId (5-min cache). All 81 server `console.error`/`console.warn` sites across 43 files converted (client code + `console.log` deliberately untouched); sentence-style event codes normalised to snake_case; shop attribution added wherever a shopId/domain was in scope. Page: range pills (24h/3d/7d/14d), error/warning/stores-affected tiles, **top failing events** triage table (click an event to filter), filtered entries table (level/event/store, 200-row cap stated in the UI, expandable context JSON). Compliance: `cleanupShop` deletes `app_logs` and `verify-compliance` now asserts zero rows across **32 tables** (was 31); the fire-and-forget race is backstopped by the nightly purge of logs belonging to uninstalled shops. Verified: `npm run logs:check` 18/18 (attribution, domain resolution, PII redaction, size ceiling, rate cap, purge), verify-compliance ALL PASS, typecheck 0 · lint 0. **Deliberate non-goal: no merchant-facing activity/audit log** (user decision) — merchants have spec-14 analytics; revisit only if spec-18 team accounts create real "who changed what" ambiguity. Sentry/hosted error tracking stays out of scope for now (console mirror + host log viewer covers app-is-down). |

**Suggested order** (from 00-overview): 01 → 02 → 03 → 05 → 06 → 09 → 04 → 07 → 08 → 10 → 13 → 15 → 16 → 17 → 11 → 14 → 12.

## Phase 0 checklist (spec 01 acceptance)

- [x] docker-compose + .env(.example) written — **container start pending Docker Desktop install (user installing)**
- [x] Prisma → Postgres, full schema, init migration w/ pgvector + HNSW + GIN (SQL generated offline via `migrate diff`, custom SQL appended)
- [x] `app/lib/` scaffolding (env, tenancy, llm, embeddings, search, pipeline stubs, ingestion, jobs, sse)
- [x] toml: demo blocks removed, scopes `read_products`, webhooks (products/collections/compliance), app_proxy, api_version aligned to 2026-07
- [x] Webhook routes (enqueue-only) + uninstall cleanup
- [x] proxy.ping + proxy.chat skeleton (echo pipeline)
- [x] Theme extension shell `chat-widget` (launcher + probe + echo-chat client)
- [x] Container healthy (pgvector 0.8.6, port 5433); migration applied cleanly; HNSW ×4 + GIN + generated tsvector column verified in DB (2026-08-06)
- [x] Seed pass (40 products, 30 knowledge docs, 6 curated answers, persona, guardrails — pseudo-embeddings, no OPENAI_API_KEY yet)
- [x] Smoke pass: pgvector, custom-index guard, hybrid search, keyword search, browse fallback, curated match, RAG (2026-08-06)
- [x] typecheck / lint / build pass (2026-08-06)
- [x] Migration drift-guard built & tested: `npm run migrate:new` + `scripts/scrub-migration.ts` (see decisions log)
- [x] `shopify app dev` boots; install on dev store OK (jgw-check.myshopify.com, 2026-08-07)
- [x] **SSE probe result: STREAMING WORKS — no fallback transport needed.** Measured 2026-08-07 via `GET /apps/ccwidget/ping` through a Cloudflare quick tunnel: frames at `11:05:08.266`, `11:05:08.767`, `11:05:09.267` — ~500ms apart, `content-type: text/event-stream`, delivered incrementally. The template README's "Cloudflare tunnels buffer the stream" warning did **not** reproduce. Spec-01 fallback seam (`window.__ccDirectStream`) stays unused.
- [x] Widget renders on the storefront and boots against the app proxy (`widget-config` → 200 JSON, `active:true`)

### Remaining manual steps (need Shopify CLI login)

```
npm run dev            # press P → install on dev store
# Theme editor → App embeds → enable "ChatConvert Chat Widget" → view storefront
# Click the "💬 ChatConvert (setup)" launcher → open browser console:
#   [ChatConvert probe] frames ~500ms apart? → record SSE result above
#   [ChatConvert chat] echo tokens streaming? → transport round-trip OK
# Add OPENAI_API_KEY to .env, then re-run: npx prisma db seed && npm run smoke  (semantic assertions)
```

## Decisions log

| Date | Decision |
|---|---|
| 2026-09-07 | **iPhone soft-keyboard bug in the storefront widget — root-caused and guarded, after three partial fixes.** User: "z index issue comes… chat window placement is fluctuating… you have update the chat multiple time but still we are getting problem". It was never a z-index problem. **Root cause:** iOS does not shrink the LAYOUT viewport for the keyboard — it shrinks the VISUAL viewport and slides it up to reveal the focused field. `.cw-panel` is `position: fixed; inset: 0`, laid out against the LAYOUT viewport, so it stays where the screen used to be while the visible area moves out from under it; every settle/rubber-band of Safari's scroll moves it again. Every earlier pass wrote only `syncViewport`'s **height**, which resizes the box and moves it not at all — and the `visualViewport` **scroll** listener was already bound to that height-only function, so the one listener that could have corrected the offset was a no-op. Compounding it: `lockBodyScroll` used `overflow: hidden`, which is not a scroll lock on iOS, so Safari scrolled the document to reveal the composer and dragged the panel with it. **Fixed:** `syncViewport` now drives the whole box from `visualViewport` (`offsetTop`/`offsetLeft` as origin, `width`/`height` as size, `bottom`/`right` → `auto` because `inset: 0` + a height is over-constrained); writes coalesced through `requestAnimationFrame` (cancelled on unbind) so the panel never trails a paint; the body lock now pins `position: fixed` with the scroll offset stashed in `top` and restored via `scrollTo` on close (the reason it was avoided before — losing the merchant's scroll position — is bought back); `closePanel` clears all six inline properties, not just height; new `cw-kbd` class (focusin/focusout, `relatedTarget`-guarded) drops `safe-area-inset-bottom` while the keyboard covers the gesture bar; `.cw-body` gains `overscroll-behavior: contain` so the rubber-band cannot chain to the storefront. **Guard:** `scripts/qa/widget-viewport.test.ts` — 31 static checks (no server, no DB), TEST-CASES area X. Verified by mutation: replacing `vv.offsetTop` with a constant fails X-1 and nothing else. Written because the failure is invisible on desktop **and** on Chrome for Android, including DevTools device emulation — which is why B4.15 kept passing while real iPhones did not. `interactive-widget=resizes-content` (Chrome/Firefox) is unchanged and still does the work there. Widget budget after: 24.48 KB gzip of 30. **Needs a real-device check on an iPhone** — emulators cannot reproduce it |
| 2026-09-07 | **Shop-domain login restored on the public landing page; `SHOPIFY_APP_STORE_HANDLE` wired to the effective config.** User compared `/` against their live ZipETA landing page and asked for the same two entry points. **Reversal of a 2026-08-21 decision:** the template shop-domain form had been deleted from `_index` and `/auth/login`, and `shopify.login` un-exported, citing "App Store review requirement 2.3.1 — no manual .myshopify.com entry". That citation was never verifiable — the 2026-08-21 PROGRESS entry itself records that a shopify.dev search could not find the rule — and a second search on 2026-09-07 found nothing either; Shopify own app template ships this exact form and documents `shopify.login` as the way to build it. Meanwhile the only other way in was the App Store link, and the listing is **not published** (`apps.shopify.com/chatconvert-app` → 404), so the landing page offered no route into the app at all. **Built:** `login` re-exported from `shopify.server.ts`; `_index` gains an `action` that calls `login(request)` (throws a redirect to the managed-install screen; a bare handle is normalised to `.myshopify.com`) and a card with a labelled shop-domain field, a busy "Redirecting…" state, and `MissingShop`/`InvalidShop` mapped to merchant-facing copy rendered in place; `/auth/login` no longer dead-ends — with `?shop=` it completes the install, without one it sends you to the card. **Separate real bug fixed:** `_index` read `process.env.SHOPIFY_APP_STORE_HANDLE` directly, so a handle set in Admin → Settings never lit the App Store link up; it now reads `runtimeConfig().appStoreHandle` like every other consumer. The listing link moved out of the hero card into its own band ("Install from the Shopify App Store"), which falls back to a plain note while the handle is blank. QA follow-through: `routing.test.ts` J-10 inverted (asserts the field exists on `_index` and that **no embedded route** has one), `TEST-CASES.md` J-10, matrix row + defect D-30 marked reversed, and `APP-STORE-REVIEW.md` blocker **B2 withdrawn** with a note to demand exact wording if a reviewer ever does cite 2.3.1. **Follow-up same day:** the App Store link was still dark because the listing slug was never `chatconvert-app` — the app has been **live since 2026-07-22 at `apps.shopify.com/chatconvert-2`**, and the toml `handle` (which is the App Home *admin URL* slug, a different field) had drifted. `DEFAULT_APP_STORE_HANDLE = "chatconvert-2"` added to `app/lib/review.ts` and chained last in `runtimeConfig().appStoreHandle`, so the landing-page button and the Settings "Leave a review" link both work with no env var; dashboard and env still override. |
| 2026-09-04 | **Agent-quality round on the real catalogue; two fixture/behaviour findings.** The golden eval only ever measured the 43-product seed fixture, so nothing measured the ANSWER on a real merchant's catalogue. Added `scripts/qa/agent-quality.test.ts` (`npm run eval:quality`, cases W1–W26 in TEST-CASES.md): 26 checks against `jgw-check`'s 167 mixed products — cross-category precision (cosmetics vs crystals in one shop), intent-level asks where the benefit lives in the title tail, literal stone names, stated budgets, catalogue honesty, the support lane, and a two-turn budget carry. **26/26 pass**; golden 19/19; storefront 226/1 (the 1 is trycloudflare SSE buffering, not the app); handover 197/0; smoke and model-portability clean. Two things it turned up. **(1) Curated fixtures were left `draft`** — the eval parks the 14 `[qa-fixture]` curated answers and republishes them in a `finally`, and a killed run had left them parked, so `storefront.test.ts` failed three curated cases against a shop with no published curated answers. Republished; the park/restore round-trip was then re-verified end to end. Check `curated_answers.status` before believing a curated failure. **(2) `PICKS: none` is overridden by a generic category noun** — for "do you have a tiger eye bracelet" (out of stock) the model correctly said *"we don't have a tiger eye bracelet available right now"* and picked nothing, then `lexicalAnchor` (`index.server.ts:944`) fired on the router keyword *bracelet*, which every candidate carries, and the mechanical tier showed four unrelated bracelets beside that sentence. The rule exists so a genuine name match cannot be contradicted by a stray "none"; it just does not separate the head noun from the discriminating attribute. Left as-is — the prompt is frozen and the copy is still honest — but logged as the next relevance decision. Also worth knowing when reading any result from that store: **126 of its 167 products are out of stock**, and `excludeOutOfStock` defaults on, so a correct agent looks like it has no catalogue. **(3) The same fixture bug, one step worse.** Re-running every non-HTTP suite turned up `"yearlyBilling": false` persisted in `app_secrets["admin:plans"]` — `overage.test.ts` withdraws annual billing to prove the subscribe action refuses a yearly interval and restores its snapshot in a `finally`, and an interrupted run had left it off, which switches annual billing off for **every tenant**. Removed the field (absent = the code default, on); overage went 58/2 to **60/0**. Both this and the curated incident are one bug — a `finally` does not run when the process is killed — so `scripts/qa/preflight.ts` (`npm run qa:preflight`) now checks the four pieces of shared state a suite can leave dirty (parked curated fixtures, `admin:plans` overrides, `platform:ai` temperature/maxTokens, throwaway shops) and exits non-zero if a run cannot be believed; `--fix` restores what is safe, never deleting shops that a live run might own. Verified by dirtying the state and watching it detect, fix and re-verify. Full re-run this round: golden 19/19 · agent-quality 26/26 · model-portability **97/0 including `--live`** (every dropdown model answers; strict-JSON routing survives a hostile global temperature 1.9) · overage 60/0 · handover 197/0 · availability 96/0 · cache 31/0 · plan-gates 25/0 · promo-codes 89/0 · subscription-webhook 12/0 · tenancy-race 10/0 · polaris-events 5/0 · trial, logs, install-lifecycle, smoke clean · features 207/1 where the 1 is `O1..O3 SKIPPED — dev server not reachable`. typecheck 0, lint 0. |
| 2026-09-04 | **Live-traffic batch: first-message latency, shopper identity, phantom links, cart on Horizon, exact-title recall.** Nine issues raised off the first week of production traffic (`ankastra.myshopify.com`); every root cause below was established from the production database and a traced pipeline run, not from reading code. **(1) Latency.** Prod: first reply median 5.8 s against 4.1 s for later ones, and the *chat* lane — which does the least work — had the worst p90 at 20.7 s. A traced turn was 5,254 ms: the router (1,152 ms) sat queued behind an embedding it does not use, and `meaningScan` + `recommendationMatch` each spent ~600 ms lazily embedding the shop's banned topics / recommendation triggers INSIDE the shopper's turn. Fixes: router + history + moderation + shopper-context now start at the top of the turn (a turn that short-circuits before the router discards one ~$0.0002 call — well under 5% of turns); the three vector layers run concurrently; `warm.server.ts` fills all four caches from `/proxy/widget-config`, which the widget fetches when the panel OPENS, seconds before anyone types. Traced result 5,254 to 3,098 ms cold, router 1,152 to 0 ms. Added a `chat_turn_slow` warn log (stage names + ms, no shopper text) so this is measurable in production instead of inferred. **(2) Shopper identity.** The agent told a shopper "I don't have access to personal information" seconds after they had filled in the pre-chat form — the Contact row was joined to that very conversation and nothing ever read it. `shopper.server.ts` appends a SHOPPER block (first name, returning-customer flag, live cart, product page) to the persona prompt for every lane. Email / phone / address are never put in the prompt at all, which is a stronger guarantee than instructing the model not to repeat them. **(3) Phantom links.** "Just click on the 'Track' navigation" turned out to be quoted faithfully from the MERCHANT'S OWN knowledge base — but no such navigation exists, order tracking lives inside the widget, and AI bubbles rendered as `textContent` so a link would have been dead anyway. `actions.server.ts`: the model emits a leading `ACTION: <key>` line (same trick as `PICKS:`; `splitLeadingLine` is now shared by both), code renders the button and owns the destination through the existing `#cc-track` deep links. Bot bubbles also render `**bold**` and safe links now — shoppers were being shown literal asterisks. **(4) Cart.** Add-to-cart had no pending state (people clicked twice and added twice) and the drawer path was Dawn-only (`cart-drawer.renderContents`), so Horizon fell through to a full page navigation out of the chat. Now `Shopify.actions.updateCart` / `openCart` (standard storefront actions, present on every Liquid storefront) with the old path as fallback, plus a spinner. **(5) Exact-title recall.** For "ruling number 5" the one correct product scored 2.00, LAST of nine: `Ruling No - 5` indexes as {ruling, 5} because Postgres drops "no" as a stop word, while `Ruling Number 1` matched the phrase in its own title at 4.00. Migration normalises `no.` / `no -` / `#` / `num` + digits to `number <digits>` inside `searchText`, mirrored in JS by `normalizeNumberIds` for query terms; the right product now leads 7.00 against 4.00 and is alone in its relevance tier. Embeddings deliberately untouched (`productEmbeddingText` feeds contentHash, so normalising there means a full re-embed for a gain the keyword lane already delivers). **(6) Memory.** `Contact.visitorId` (localStorage, no expiry) separates identity from the 30-minute BILLING session, which had also been acting as a memory boundary; a new conversation seeds its summary from the same contact's last one within 14 days; and summary refreshes now fold the previous summary in, so a thread longer than the 50-row lookback stops silently forgetting its own opening. **(7) Cosmetic.** The chat thread sits on a 4% wash of the merchant's brand colour instead of near-white bubbles on near-white ground; the human-mode placeholder "A team member will reply here…" became "Message the team…" (a placeholder is an instruction to the person typing, and that status is already in the thread as `afterHandoverMessage`). |
| 2026-09-03 | **Reply text quoted "$" on an INR store (user: storefront cards show Indian currency but the chat message says US dollars; app must work worldwide).** The widget and inbox already format card prices with the shop currency (theme-side / `ChatProductCards` Intl) — the one wrong surface was the MODEL's candidate payload, which carried bare numbers (`"price": 1499`), so any price the model voiced defaulted to "$". Fix: new `app/lib/format/money.ts` `formatMoney(amount, code)` (Intl narrowSymbol, integer prices without decimals, never throws — unknown code → "CODE 9"), and the buy-lane allow-list now sends `price: formatMoney(c.price, config.currency)` (`shops.currency` synced at install; "USD" fallback). Verified on jgw-check (INR): "how much is the pyrite bracelet?" → "priced at ₹1,499" (question → catalogue rescue → card + correct price), "show me black bracelets under 2000 rupees" → "under ₹2000", price_max 2000 honoured. Golden re-run green. **User decision: ship single-currency-per-store** (auto-detected at install, no setting). Shopify Markets / multi-currency stores show the BASE currency in chat — deferred; the correct build is per-market prices via Admin `contextualPricing` keyed by the shopper's storefront currency/country (the theme block already passes `data-country`), never a symbol swap on base-currency amounts. |
| 2026-09-03 | **Reply language is now enforced — the settings existed, nothing read them (user: agent takes 2-3 messages to switch to the shopper's language; should switch on the first message and mid-chat).** Root cause: `persona.defaultLanguage` / `autoDetectLanguage` were written by the AI-instructions screen (with the `multi_language` plan gate on save) but no generation call ever saw them — spec 08 had listed "multi-language reply enforcement" as out of scope — so the model followed the English system prompt and store data and only mirrored a non-English shopper once enough foreign history accumulated. Fix: `languageInstruction()` in prompts.ts, appended to the persona prompt for every generation lane (chat/buy/question) with a `language` trace step. Auto-detect ON: "reply in the language of the shopper's LATEST message … switch immediately when the shopper switches". **And `multi_language` is un-gated entirely (user decision, same day): auto-detect language is available on every plan** — removed from `GatedFeature`/`GATED_FEATURES`/the Plus feature list, from the save-path `requireShopFeature` check, from the instructions-page lock UI (`multiLanguagePlan` prop gone), from the /admin/plans label map and plan-usage bullets; stored plan overrides naming it are tolerated (planPatchSchema filters unknown features). plan-gates B-15 now asserts the save SUCCEEDS on Free; admin-check assertions updated; spec 08/15 + TEST-CASES aligned. Auto-detect OFF: "Reply ONLY in {default}, no matter which language the shopper writes in" — the polite version ("Reply in English.") measurably lost to the model's mirroring instinct. Router prompt additionally asks for keywords in English so a non-English (incl. Devanagari, which the message-word tokenizer strips) message still hits the English catalogue's keyword lane. Verified live on jgw-check (auto-detect ON): EN→ES→HI switches within ONE conversation, each reply in the latest message's language with correct cards; Spanish first message → Spanish reply + the black bracelets; dev-shop (auto-detect OFF) now answers Spanish/Hindi shoppers in English as configured. Golden re-run after the prompt tuning: 19/19. Known limitations (documented, not built): canned strings (fallback/clarify/busy/cap, curated talking points) stay in the merchant's configured language, and RAG recall is weakened cross-language ("livrez-vous au Canada ?" scores below minMeaningScore against the English shipping chunk → fallback; needs translate-then-retrieve or multilingual knowledge). |
| 2026-09-03 | **Overage billing made real: nothing is served unpaid, nothing served goes unbilled** (user: "if cap is over then start charge per conversation and get paid at the time of payment"). An audit of the existing path found the happy path correct — Free and yearly hard-cap, monthly+usage-line keeps answering and charges $0.40 — and money leaking at both ends. ① **The $100 `cappedAmount` was a silent free ride.** Shopify refuses a usage record past the merchant's approved ceiling (docs: "Failed to create usage charge"); the app logged that and kept answering. New `usage-cap.server.ts` reads `balanceUsed`/`cappedAmount` off the subscription line (cached 60s, mock-mode derived) and `aiAllowed()` now refuses a billable conversation with no headroom — the AI stops exactly where the money stops. **Fails safe**: an unreachable Shopify counts as headroom, because cutting a merchant's chat over our own API hiccup is the worse failure. ② **Failed reports were lost revenue.** The call is fire-and-forget, and `overageCount` was already incremented — so a blip meant work served that could never be billed. Added `PlanUsage.overageReported` (migration `20260903091338_plan_usage_overage_reported`, additive, default 0): the difference from `overageCount` IS the debt, `submitOverageRecords()` bills exactly that and increments on acceptance, and the new hourly `overage-reconcile` job drains it. Re-running charges nothing, so retrying forever is safe. ③ **`overageCount` was write-only** — a merchant could be billed with nothing on screen. `usageStatus()` now drives three Plan & Usage states in the order they happen: ≥80% of allowance, past the allowance (count · rate · spend so far), ceiling reached (critical + "Raise limit to $250"). ④ **Raising the ceiling** via `appSubscriptionLineItemUpdate` → confirmation URL → top-level redirect, same shape as subscribing; steps $100/$250/$500/$1000. Both GraphQL operations were validated against the 2026-07 Admin schema before use. Verified by the new **`scripts/qa/overage.test.ts` — 47 checks, all passing**: metering rules, tenancy, 5-way concurrency, bill-exactly-once, retry-after-failure, ceiling stop, raise, the three warning states, Free/yearly/no-usage-line hard-caps, open mode, month rollover. `test-billing-mock.ts` green too (it now proves a yearly sub bills nothing and keeps the debt). **Deliberately NOT changed:** trial shops are still metered and billed (Shopify puts trial-period charges on the next invoice) — that is a pricing decision, not a bug, and it is now written down. **Still pending manual:** no `appUsageRecordCreate` has ever run against real Shopify billing; every pass is mock mode. ⑦ **Annual billing became an operator switch** (user: "if we unset the pricing of yearly from plans then will the yearly feature hide?" — no: zeroing the price leaves the toggle in place and the cards reading "$0/year", with a live subscribe button). New `yearlyBilling` flag beside `enforcement` in the matrix, default ON. Off hides the Monthly/Yearly toggle AND makes the subscribe action refuse `interval=yearly` — the guard is the action, since the interval is a form field a stale tab could still POST. Existing annual subscriptions are untouched, and a shop already on annual keeps seeing its own interval. The reason it is worth having: Shopify allows usage charges on monthly cycles only, so a yearly subscriber can never be billed for extra conversations; they hard-cap, and the user spotted that the obvious answer (upgrade prompt) dead-ends on Plus-yearly, where no higher tier exists. Withdrawing annual makes every paying shop monthly, which is the only interval where overage works end to end. 6 checks added (60 total). ⑥ **Open enforcement stopped meaning "unlimited"** (user: "unlimited is not good — if enforcement is disabled we can provide the features which the Plus plan defines"). `getQuota` used to return MAX_SAFE_INTEGER in open mode: no ceiling at all, and meters that would read "0 of 9,007,199,254,740,991". Open now grants `OPEN_MODE_PLAN` (Plus) as the LIVE matrix defines it — `max(top tier, the shop's own plan)` per dimension and the union of both feature lists, so it can only ever be an upgrade even if someone mis-edits Plus. Consequence written into the admin banner: a monthly paid shop that passes the TOP tier's conversation cap in open mode IS billed overage at its own rate — generous, not free-for-all. Test suite extended to 54 checks, pinning "a real number, not MAX_SAFE_INTEGER", "never a downgrade", "a genuinely unlimited dimension stays unlimited", and the ceiling at the top tier's quota. `OPEN_MODE_PLAN` lives in plan-shared.ts, not plans.server: the admin UI names the plan, and the bundler rejects a server import in client code (typecheck did not — the build did). ⑤ **Same day, user follow-up:** an operator had set a $0.50 overage rate on FREE in `/admin/plans` and the card advertised it — but Free can never be billed (no subscription, no usage line), and neither can any YEARLY plan (Shopify rejects usage lines on annual subs), so the card was making an unkeepable promise on two of its four tiers. The rate stays editable at /admin/plans but for the PAID tiers only — Free has no field, and applyConfig() ignores a rate stored against `free` whatever writes it (verified: forcing 0.5 onto free leaves the effective rate null, while Pro accepts 0.75 live). The plan card prints the line ONLY in monthly mode on a plan that has a rate, the same predicate the server bills on. Overage stays disclosed where it is actually true: the Shopify approval page usage terms, and the Plan & Usage FAQ, which checks `overageBillable()` first. |
| 2026-09-03 | **Admin console round 2: invited operators, two feature switches, a theme, and a glass redesign.** ① **Operators are two kinds now** (user: "details for admin login will remain same but we can create new user inside the admin panel also"). ROOT is the `.env` pair — compared against the environment on every sign-in, never stored, undeletable, and its `platform_admins` row is a mirror with an EMPTY `passwordHash` (which is also how the row is recognised as env-managed). INVITED accounts are created at `/admin/access` with a real scrypt hash and are **not** touched when the `.env` pair changes; only stale env-managed rows and root's own sessions are dropped then. `/admin/access` regained add / remove / change-my-password (root can do none of those to itself — its password is in `.env`, and the page says so). Verified over HTTP on a built server: invited account created → listed → signs in (302 + `cc_admin`) → removed → its session cascaded away; root email refused as a normal account; root row refused for removal. ② **Coupons master switch** at the top of `/admin/promo-codes` — runtime flag `promoCodesEnabled`, enforced in `validatePromoCode()` and not merely hidden, so a merchant holding a code cannot redeem it past the switch. ③ **Per-plan visibility switch** at the top of each tier in `/admin/plans` — `PlanDefinition.hidden`, saved with the rest of the tier. Hidden plans leave `offeredPlans()` and are never named by `requiredPlanName()` / `nextPlanNameForQuota()`, **but a shop already on one keeps its quotas, keeps being billed and still sees it as its current plan**: hiding withdraws an offer, it never downgrades anyone. ④ **Light / dark / auto theme** in the rail and top bar, stored in `cc_admin_theme` (Path=/ — the `/admin.data` RFC 6265 trap again) and read by the `admin.tsx` loader so the FIRST paint is right: no flash, no blocking script. Verified all four cookie values including the bogus→auto fallback. ⑤ **Glassmorphism, mobile-first redesign** (user: "the ui of admin pannel is too basic"). The console stopped borrowing `web-shell.css` — that 2026-08-20 "must look like the merchant web app" rule is superseded for `/admin` only, and splitting the sheets is what let the merchant surfaces stay untouched. New `admin.css` (`cca-*`) + `AdminUi.tsx` (`AdminPage`, `AdminCard`, `AdminStat`, `AdminToggle`, `AdminSegmented`, inline-SVG `Icon`): token-driven light/dark (never a colour defined only inside a media query), fixed radial blooms under `backdrop-filter`, phone-first layout with an off-canvas rail promoted at 900px, `env(safe-area-inset-*)`, tables that pan in their own scroller, and a `@supports not (backdrop-filter)` fallback. **Polaris `s-page`/`s-section` had to go** — pure containers, in shadow DOM, unreachable from our CSS — but form CONTROLS stayed Polaris; cards holding them take `variant="form"`, which keeps a light frosted sheet under the controls in dark mode (dark shell, light working surface). All 8 authed pages verified 200 with the new markup on a built server. **No migration** in any of this. ⑥ **Follow-up the same day (user):** dark-mode cards were unreadable — Polaris `s-*` renders LIGHT chrome from inside shadow DOM and it is in every card, so a dark card put dark Polaris ink on a dark ground. Cards are now light frosted sheets in dark mode (one rule for ALL cards, not an opt-in variant: the failure is silent, and a card that gains a Polaris child later would go dark-on-dark unnoticed), and the sheet re-declares the ink TOKENS rather than just `color`, since every descendant reads `var(--cca-ink*)`. Also: the theme control was unreadable in light mode — frost-on-frost is ~4% apart — so the selected pill is now FILLED with the brand gradient; and the coupons / plan-visibility switches were rebuilt as Polaris `<s-switch>`, matching the plan-enforcement switch, with the custom `AdminToggle` deleted rather than left as a second switch design. In-page buttons on /admin/access are Polaris `<s-button>` too; `.cca-btn` now survives only for the rail sign-out, where Polaris cannot inherit the frosted chrome ink. |
| 2026-09-03 | **The operator console is `/admin`, its credentials are the `.env` pair, and the support bubble is merchant-only.** ① **`/platform` → `/admin` everywhere** (user: "make the platform url to admin because its a admin panel … platform word should not be there across the app"): 12 routes, `app/lib/platform/` → `app/lib/admin/`, `PlatformShell` → `AdminShell`, `requirePlatformAdmin` → `requireAdminUser`, Prisma models `PlatformAdmin`/`PlatformSession` → `AdminUser`/`AdminSession`, cookie `cc_platform` → `cc_admin`, scripts `platform-*.ts` → `admin-*.ts`, spec `19-platform-admin.md` → `19-admin-panel.md`. **The physical tables stay `platform_admins` / `platform_sessions`** (`@@map`) — renaming them is a data migration for zero user-visible gain, and Prisma emits a model rename as drop+create, which would delete the accounts. No `/platform` redirect stub: re-adding the word was the thing being removed. ② **Sign-in now reads ADMIN_EMAIL / ADMIN_PASSWORD from `.env` and nothing else** (user: production accepted a password that was in neither `.env`). Cause: accounts were rows in `platform_admins` and the env pair was a one-time bootstrap, so production's row outlived the variable that created it. Now `verifyAdminLogin` compares against the environment with `timingSafeEqual` and never touches a hash; the row is a mirror (`passwordHash` stays `""`) kept only for the session FK + lockout counters; an HMAC fingerprint of the pair lives in `app_secrets["admin:credentials"]` and `readAdminSession` checks it **before** the cookie — so a `.env` edit + restart deletes every session and every other operator row on the first request, and blank values disable sign-in without destroying anything. Dev and production diverge freely, each reading its own file. `/admin/admins` (add/remove/change-password) became **`/admin/access`**: read-only identity + "sign out other sessions", because a management UI that cannot grant a login is a lie. `scripts/admin-account.ts` deleted; `admin-lockout-probe.ts` now also proves a database-only row is refused. **No migration** — the change is behavioural. ③ **Progryss' support widget moved out of `root.tsx` into `components/SupportChat.tsx`, mounted only on the embedded branch of `app/routes/app.tsx`** (user: "i only want to run this inside the embedded iframe"): `/admin` is our own team, `/web` is the merchant's agents, and the storefront carries the merchant's OWN widget. Rendered as markup, not injected in an effect, so the loader's `document.currentScript.dataset.appId` resolves. ④ **The two "we'll set it up for you" cards were false** — we don't do merchant setup (user). AI Agent and Plan & Usage now offer help WITH their setup, `mailto:` replaced by a chat IconChip pointing at that bubble. `PlanFaq`'s "Reach out to us" mailto is still there; out of scope. |
| 2026-09-01 | **Product recommendations were worse than Shopify's own search — four root causes, all fixed (user: "results are too poor for product recommendations, even Shopify default search works better").** Measured on the user's real store jgw-check (Ankastra crystal bracelets: 47 purchasable products, 6–8k-char SEO descriptions) with `npm run trace`: 6 of 9 realistic asks were wrong. ① **Field-blind keyword coverage.** A query word deep in the prose counted the same as one in the title, and every bracelet's description names other stones and colours ("pairs with black outfits", "recharge on a selenite plate", "place it near a clear quartz crystal") — so "black bracelets" carded Moonstone/Pyrite/Hematite, "selenite bracelets" carded Pyrite/Howlite/Moonstone, "clear quartz" carded Rose Quartz first. Fix: coverage is now field-aware in SQL — title/type/vendor/tags hits (tsvector weights A+B via `ts_filter`, computed once per row behind an `OFFSET 0` fence) count in full, description/metafield-only hits count `DESC_WEIGHT` 0.4; `headTerms` records where each word matched; `ts_rank_cd` normalisation 1 damps the long-prose bias; the shopper tier no longer double-counts the plural of a router word; `selectRelevant` keeps everything within `TIER_MARGIN` 0.5 of the top (a title-vs-prose hit on one word = 1.2 apart). ② **The vector lane was dead** whenever the keyword lane filled the list (vector-only rows sorted last and were cut). Fix: 3 of the 8 candidate slots are reserved for vector-lane rows (`withVectorReserve`) — the mechanical tier is unchanged, the MODEL sees them. ③ **No judgement step**: cards were the mechanical tier, so a wrong candidate could never be dropped. Fix: `picks.server.ts` — the reply model gets the 8 candidates with ids and snippets (`in title/type/tags:` / `in description:` + fragment) and opens its reply with `PICKS: 3, 1` or `PICKS: none`; the line is parsed off the stream before the prose reaches the widget and becomes the cards (allow-list ids only). Still 2 chat calls. Two lexical belts because gpt-4o-mini pads towards four (it picked ALL 8 for "black bracelets") and once said none against the evidence ("a bottle that keeps drinks hot" with an Insulated Water Bottle listed): when some candidate carries every router keyword in its head — or the top candidate alone (within its tier) satisfies every router keyword anywhere ("february" is description data) — picks may only narrow the tier; `none` stands only when no candidate carries a router keyword in its head. ④ **The router invented policies**: "which bracelet is good for money and wealth" → `off_topic` with NO store scope configured; "something to help with stress and anxiety" → blocked as "medical advice" (then `off_topic` on the next run); "something that blocks rfid" → blocked citing "weapons" (or literally "BANNED TOPIC") about half the time. Fixes: `off_topic` ignored unless persona.scope is set (mirrors the 2026-08-17 blocked rule); `blocked` honoured only when `blocked_reason` names a configured topic (`configuredTopicNamedBy`, prefix-tolerant) AND a 3-token yes/no confirm (`blockConfirmUser`: "advice about the topic, or a product to buy?") agrees — embedding similarity was tried as the second check and rejected by measurement (rfid↔weapons 0.23 vs "cure my arthritis"↔medical advice 0.28); router prompt clarified (asks for a product for a need/purpose are `buy`, never a banned topic). Plus a **question → catalogue rescue**: a `question` with no knowledge hit runs product search over the shopper's words and, when the keyword and vector lanes agree, hands the turn to the buy lane ("how do I clean my bracelet" → `PICKS: none` → merchant fallback + unresolved queue, exactly as before; "is amethyst good for anxiety?" stays blocked because the merchant banned medical advice — their call). **After**: all 9 real-store asks return only fitting cards (black → Black Obsidian + Black Rutilated; evil eye → Black Obsidian; money → Pyrite + Hematite (+Citrine); stress → Howlite/Amethyst; february → Amethyst; selenite → the 4 selenite products; clear quartz → Clear Quartz). Golden set extended to **19/19** (3 seeded bracelets reproduce the prose-mention trap + `rejectCardTitle` precision asserts); features suite +9 cases (S12–S15 field-aware coverage, P1–P4 picks protocol, G1 topic naming). Seed hygiene: `prisma db seed` now removes the pre-D5 `gid://seed/Product/*` rows (they had doubled every title and every curated answer) and exits instead of idling on the keep-alive agent; run `scripts/qa/seed-curated.ts` after it to restore the QA fixtures. Spec 03 / ai-pipeline skill / TEST-CASES D-01, D-14–D-17 updated. Note for the merchant: the "chat assistant is offline" replies in the inbox on 08-26/27 were the AI toggle being off, not a defect. |
| 2026-08-31 | **Three merchant-reported widget fixes, plus in-panel deep links.** ① **Inbox product cards wrapped one-per-row** instead of scrolling like the storefront. `ChatProductCards` now mirrors `.cw-cards`: `flex-wrap:nowrap` + `overflow-x:auto` with a hover-revealed thin scrollbar. Alignment is `align-self`, deliberately NOT `justify-content:flex-end` — AI messages (the ones carrying cards) render on the out side, and end-justification on an overflowing flex row strands the leading cards before `scrollLeft: 0` where no gesture can reach them. ② **"App is embedded to your theme" contradicted itself** — badge "On" beside a "Turn on" button — and stayed stale after the merchant disabled the embed in the customizer. Root cause proven against the dev DB, not guessed: `jgw-check` had `read_themes` granted AND `widgetSeenAt` set, so the 7-day traffic heuristic (D-2026-08-26) short-circuited before the authoritative theme read. The theme read is now consulted FIRST and traffic only fills the gap. The button label follows the real state; there is **no** "Turn off", because Shopify's theme-editor deep link has only `activateAppId` and no deactivate parameter — honest labelling beats a control that cannot work. A Recheck button was built and then **deleted on user instruction** ("why need the recheck button, always have the correct info"); freshness is caller-controlled instead (`getEmbedDetail(shop, { fresh: true })` on Settings, a 30s damper for the dashboard, which revalidates every 5s). ③ **Draft themes.** Merchants install apps on an unpublished theme first, and detection queried `roles: [MAIN]` only, so that setup reported a flat "Off" — telling the merchant to redo work they had already done. The widget itself was never affected (`chat-widget.liquid` has no `design_mode`/preview gate and the proxy serves preview URLs), so this was a reporting bug, not a functional one. New fourth state `draft`: badge "Draft theme only" (warning) naming the theme, dashboard step relabelled "Embed app to your LIVE theme (currently on a draft theme)" and still counted `todo`, since shoppers genuinely cannot see it. The `UNPUBLISHED`/`DEVELOPMENT` query (validated against the 2026-07 schema) runs ONLY when the published theme lacks the embed and is bounded to `first: 10`, because it fetches a whole `settings_data.json` per theme; `shopify.app.toml`'s `read_themes` justification was updated to disclose it, since the reviewer reads that text. ④ **Widget deep links.** An FAQ answer could tell a shopper to "click Track" but had no way to send them there — `sanitizeHtml`'s `SAFE_URL` allowed only `https://`, `/`, `mailto:` and `tel:`, so a fragment href was stripped to a dead `<a>`. `#cc-track`, `#cc-chat` and `#cc-open` are now allow-listed (fragment-only, so they cannot navigate or execute) and `chat-widget.js` intercepts them through ONE delegated document listener — which means they work from any merchant-authored HTML in the panel (FAQ answers, campaign bodies, disclaimers) without each renderer wiring them up, and from the storefront itself: a theme menu item pointing at `/#cc-track` lands on the home page with tracking already open (hash consumed via `replaceState` so the same link fires again later in the session). Guarded three ways: a link to another page or host still navigates normally; `availableScreen()` falls back to home when the merchant has turned tracking or live chat off, so a stale FAQ link cannot open a screen the store no longer offers; and both the sanitizer and the rich-text editor strip `target="_blank"` from `#cc-` links, which would otherwise reload the storefront in a new tab and defeat the point. **Merchants never type the token** (user: "merchant dont know what to add in the richtext"): the editor's link row leads with a **"Link to" select** — Web address / Order tracking / Chat with us — and the URL field is replaced by a plain-English explainer when an action is chosen, so linking to tracking is select-the-word → link → pick. `PANEL_ACTIONS` is the one list the select, the insert path and the target-stripping all read, and re-opening the row on an existing link resolves back to its option instead of showing raw `#cc-track`; with no text selected, a panel action inserts its LABEL as the link text, since `#cc-track` would otherwise be shown to the shopper. Gates: typecheck 0, lint 0, build OK; sanitizer verified against injection attempts (`#javascript:`, `#cc-track onmouseover=`) — both reduce to a bare `<a>`. |
| 2026-08-27 | **Pre-deploy gate re-run: whole suite green, six harness defects fixed, no product defect found — D-24.** Full gate ahead of production deploy + submission: typecheck 0, lint 0, `npm run build` OK, 17 QA suites (2,265 checks) + 9 standalone scripts + smoke + golden eval, with `perf-queries`/`cache` run against a **full-volume** seed (2k products, 20k conversations, 200k messages, 150k events) rather than the dev shop's ~1k rows. Six red results, **all six in the test harness or its fixtures — nothing in the app.** (1) `ui-admin` "drill-down never names another store" failed because `dev-shop.myshopify.com` **contains** `shop.myshopify.com`, so a naive `body.includes(otherDomain)` reported a cross-tenant leak that was the page naming its own store; the assertion now blanks out the page's own domain first. (2) "the fleet cost tile never includes usage the merchant table cannot explain" was real but fixture-caused: `plan-gates.test.ts` embeds content, then tore its fixture down with a raw `shop.deleteMany` that omitted `llm_usage_daily` — which has no FK — leaking 7 embedding calls into the operator's fleet cost total **per run**, permanently unattributable. Production is unaffected (`cleanupShop()` deletes usage rows and is the ONLY thing in `app/` that deletes a Shop), so the fix is the teardown, plus a one-off scrub of the 4 accumulated orphans. (3) "the stored Resend key is still sealed at rest" called `.startsWith("enc:v1:")` on an **empty** string — no key is configured on this environment and nothing is stored in plaintext; the assertion now states which invariant it verified. (4) `ui-admin`'s log shop-filter assertion picked the newest log row carrying a `shopId`, which was a **purged** QA fixture, so the loader correctly dropped the filter and showed its "store no longer exists" banner while the test asserted the happy path; it now selects a row whose `shopLabel` still resolves. That failure was previously masked by suite ordering. (5) `admin-check.ts` asserted **pre-`knownFeatures` semantics**: it sent `features: ["exports"]` with no `knownFeatures`, and the three-way merge correctly treats an unlisted-and-unseen feature as "added after this operator last saved" and falls back to the plan default instead of silently gating it off. The real `/admin/plans` form always submits the full `GATED_FEATURES` list (`admin.plans.tsx:196`) and `ui-admin.test.ts:707-719` already covers these semantics — the script was simply stale, and now sends what the form sends. (6) **The golden eval was not hermetic.** `"do you ship to Canada?"` returned `curated` twice (both failures were the same input — the repeated-question case reuses it) because `scripts/qa/seed-curated.ts` publishes 13 fixtures onto the same dev shop, and its `"do you ship internationally"` scores **0.7198** — under the 0.80 serve threshold, over the 0.65 borderline — so the confirm call accepts it and the turn is served from curated. That is the product working correctly, but it silently changed what the standing AI-quality gate measured, and the fixture marker `[qa-fixture]` was being served verbatim in shopper-visible text because the curated path emits talking points with zero generation. `eval-golden.ts` now parks the `[qa-fixture]` rows as drafts for the run and restores them in a `finally` (the API-key guard was hoisted above the parking so an early abort cannot leave them unpublished), and `main()` returns its failure count instead of calling `process.exit`, which would have skipped that restore. **Golden set is now 16/16**, including the `"something that blocks rfid"` case that had flaked ~1 in 3. Also re-verified the submission blocker table against the tree and corrected it: **B2 and B4 are CLOSED** (`app/routes/auth.login/` no longer exists and `shopify.login` is unexported; `isBillingTestMode()` returns false when `NODE_ENV === "production"`) — the doc still listed both as open. B1 (PCD level 2), B3 (production URLs) and B5 (privacy policy) remain, and none of them is a code change. Two `handover` spec divergences remain reported-not-failed by design: spec 10's "2+ thumbs-down" trigger is unimplemented (only a 👎 character is detected; survey ratings never reach `detectHandover`), and repeated-question matching compares normalized exact text rather than embedding similarity, so paraphrases are missed. |
| 2026-08-27 | **Free trial is now an entitlement, granted once per shop — D-23.** Asked whether the 7-day trial worked on every plan. The mechanics did (7 days on basic/pro/plus, 0 on free; `trialEndsAt` stamped; the D-08 sweep flips trial→active), but `appSubscriptionCreate` passed the plan's headline `trialDays` **unconditionally, on every call**, and Shopify grants whatever it is asked for. So the trial restarted on uninstall→reinstall, on Basic→Pro, on downgrade-to-Free→resubscribe, and on monthly→yearly: a merchant could sit on a paid tier indefinitely without ever being charged, and the app kept no memory of it — uninstall, `shop/redact`, `cleanupShop` and the cancel webhook all nulled `trialEndsAt`. Confirmed against shopify.dev that this is OUR problem: the documented 180-day trial proration belongs to **Shopify App Pricing (managed pricing)**; this app bills through the **Billing API (manual pricing)**, which has no equivalent guarantee. **Model chosen — an absolute deadline, not a consumed-days counter.** Counting consumed days is farmable (switch plans every 23h and every segment floors to zero); instead `Shop.trialStartedAt` + `trialDeadlineAt` (migration `20260827120000_shop_trial_ledger`) record when the clock started and when the entitlement runs out, and the deadline **never moves forward**. Every later subscription is granted `ceil(deadline − now)` days. That makes the legitimate case free — a mid-trial upgrade keeps the SAME trial end date, which is what merchants expect and what Shopify's own managed pricing does, and upgrading ten minutes in still yields the full 7 days — while the abuse loop gets zero, because the anchor it is measured against does not move (proven with 200 synthetic switches). Rounding UP cannot compound for the same reason: total overshoot across any number of switches stays under one day. Operator changes at `/admin/plans` behave per Shopify's rule — raising `trialDays` extends the ORIGINAL start so days already used subtract from the new total, and lowering it never shortens a trial already running. A rolling `TRIAL_WINDOW_DAYS = 180` forgives a merchant who genuinely left half a year ago, matching the admin semantics rather than inventing stricter ones. **Bypass-proofing:** the grant is resolved inside BOTH billing providers, not at the route, so no present or future call site can subscribe a shop without consulting the ledger; the mock provider resolves the same number so mock-mode tests cannot prove the wrong behaviour. `trialLedgerAfterGrant()` carries an existing ledger forward unchanged, so a replayed callback or a redelivered webhook converges instead of extending the trial each time. The `app_subscriptions/update` handler stamps the ledger too — it is the ONLY writer when the merchant closes the tab before the callback. The four reset paths deliberately clear only `trialEndsAt` (the live subscription's trial) and leave the ledger alone; because that is the entire defence and would be silently undone by two obvious-looking lines, `scripts/qa/trial.test.ts` asserts it at the SOURCE level across all five files. The ledger also survives `shop/redact` on purpose: a myshopify domain plus two timestamps is a shop-level abuse record, not customer personal data, and erasing it would turn GDPR redaction into a free-trial reset button. **Merchant-facing copy fixed with it:** the plan cards read `def.trialDays` and would have promised "7-day free trial" to a shop Shopify was about to bill immediately — a false pricing claim (App Store req 1.1.4). They now render the days this shop can actually still get, or drop the phrase entirely at zero; the yearly branch never mentioned the trial at all despite yearly subscriptions getting one. **Coverage gap closed:** Pro had never been driven end-to-end through subscribe→callback (only Basic monthly and Plus yearly were), so its trial was untested — it is now case B1. New suite `scripts/qa/trial.test.ts` (33 checks: policy under a synthetic clock, the live flow against the dev DB, and the source guard) plus 16 new cases A-T1–A-T16 in TEST-CASES.md. Gates: typecheck 0, lint 0, trial 33/33, billing-mock ALL PASSED, install-lifecycle 104/104, features 201/0, subscription-webhook 12/0, promo-codes 89/89. `ui-embedded` shows 5 failures on `/app/plan-usage` ONLY because the long-running dev server still holds a Prisma client generated before the migration (the identical select succeeds in a fresh process) — it clears on the next `npm run dev` restart. |
| 2026-08-27 | **Five-item batch: inbox product cards, working team-member avatar, collections earn their sync, plan signals everywhere, and the web "Open in Shopify admin" link.** ① **Merchant-side product cards.** The pipeline stored `Message.productCards` and the shopper saw them, but the Inbox thread rendered the bubble text only — an agent taking over a conversation could not see what the AI had recommended. New `app/components/ChatProductCards.tsx` is now the ONE card renderer for both merchant surfaces (Inbox thread + Test AI console, which had its own inline copy); `inbox.server.ts` selects and defensively parses the JSON column (`toThreadProductCards` drops anything without an id and a title rather than trusting the blob). Deliberately read-only — "View product ↗" and no Add to cart: the merchant is not the shopper. ② **"Team member profile (coming soon)" now works.** `TeamMember.avatarUrl` (migration `20260826200000_team_member_avatar`) + `WidgetSettings.avatarMemberId`; new `app/lib/widget/chat-avatar.server.ts` is the single resolver used by the storefront config, the chatbox preview, the admin preview and the Inbox — so all four cannot drift. A member removed AFTER selection degrades to store branding instead of a blank circle, while `settings-save.server.ts` REJECTS saving `team_member` with a missing or inactive member, so the fallback only ever covers real drift, never a bad save. ③ **Collections were synced and used nowhere** — and worse, two features (custom recommendations, campaign recommendations) accepted collection targeting that could never fire, because no product↔collection membership existed in the database. New `CollectionProduct` join table (migration `20260826201000_collection_products`), enumerated from the COLLECTION side (the product sync query is already near the GraphQL cost ceiling), paged 250 with a logged `collection_membership_capped` at 2000 rather than a silent truncation, refreshed by a `collection-membership` job the webhook ENQUEUES. Both silent no-ops fixed; `customRecommendationPool` dropped its `take: 8` and now ranks by the merchant's own id order before slicing, because a 100-id collection sweep could otherwise bury the explicit picks under arbitrary row order, and campaign cards cap AFTER the status/stock filter (a third out-of-stock pick used to return two cards). The sync then earns its keep: `shopCollectionContext()` grounds category questions in the store's real collection names. Discounts, for the record, WERE already used (`activeDiscountContext`). ④ **Plan signals (spec 15).** The app said "this costs more" three different ways and, on seven gated features, not at all — the control simply failed or silently capped, and a merchant cannot upgrade for a limit they were never shown. New `app/components/ui/PlanGate.tsx` is the one vocabulary: `PlanBadge` (locked control), `PlanMeter` (countable quota — 80% warning / 100% critical, unlimited renders as a plain count because a bar against MAX_SAFE_INTEGER reads as broken), `PlanBanner` (locked or capped section). Every tier NAME comes from `requiredPlanName()` / `nextPlanNameForQuota()` reading the LIVE matrix — the operator can move a feature between plans from `/admin`, so a hard-coded "Pro" would quietly start lying; the five sites that had one ("Pro +", "Basic+", "Pro/Plus", "Upgrade to Pro", "Available on Pro and Plus plans") are converted. Coverage swept mechanically: **all 13 `GatedFeature`s and all 11 `QuotaDimension`s now have a merchant-visible surface** — including `survey` and `push_notifications`, which were enforced server-side only, so a merchant could configure a survey that never reached a shopper and an agent could enable notifications that were never delivered. Analytics ranges past the plan's window are DISABLED with the tier appended instead of silently narrowing (a short window read as a quiet quarter). ⑤ **Web nav "Open in Shopify admin" landed on the apps LIST**, not the app: `WebShell` built the URL without the API key. New pure `app/lib/format/admin-url.ts` (`adminAppUrl`) is shared with `shopify-billing.server.ts` so the two agree; the billing callback keeps its own `appUrl()+path` fallback, since the apps-list fallback would drop the query string. Gates: typecheck 0, lint 0, build ✅, `prisma migrate status` up to date (26 migrations). |
| 2026-08-26 | **Two browser-round follow-ups resolved.** ① **"Recent orders" was hard-coded to "No info"** for every conversation in the Inbox details pane — a merchant expanded it on each chat and learned nothing. Now populated from the Admin API via new `app/lib/inbox/recent-orders.server.ts`, using the `read_orders` scope the app ALREADY holds for the in-widget order lookup — no new scope, no merchant re-auth. Deliberately data-minimised for protected-customer-data level 2: it selects only what the merchant already sees on their own Orders list (number, date, financial/fulfilment status, total) and never the shipping address, phone or line items; nothing is stored, it is fetched per view. The lookup keys on `shopifyCustomerId` first, then email, and returns an empty list when there is neither — an empty Shopify order search would otherwise have returned the shop's most recent orders, i.e. SOMEONE ELSE'S. Three honest empty states replace the single dead one: real orders, "No orders yet" (identified shopper with none), and "No info — this visitor hasn't shared an email" (anonymous). Fails soft on a declined scope, a throttle or an outage. ② **"App is embedded to your theme: Unknown" was NOT a defect** — `app/lib/embed-status.server.ts` documents that detection reads the published theme's settings and therefore needs the `read_themes` scope, which this app deliberately does not request (a scope addition forces every merchant through re-auth), gated further behind an `embedStatusEnabled` runtime flag defaulting to false. Rather than buy that scope, the status is now inferred from traffic: ONLY the theme app embed calls `/proxy/widget-config`, so a recent request is positive proof the embed is live. New `Shop.widgetSeenAt` (migration `20260826190000_shop_widget_seen_at`), stamped fire-and-forget and throttled to once an hour so a busy storefront cannot turn a cached GET into a write per page view; `embedStatusFromTraffic()` is consulted FIRST inside `getEmbedStatus()`, so most shops resolve with no Shopify call at all. Deliberately one-directional — silence is NOT evidence the embed is off, since a store with no traffic looks identical to one with the embed disabled, so it returns null and falls through to the existing "unknown" rather than claiming "off". Verified end to end: null → touch → "on" → badge "on", with a second shop correctly staying null, and the fixture stamp reverted afterwards. |
| 2026-08-26 | **Pre-submission QA round 2 — surface-by-surface execution, and the biggest defect in the app's history.** Round 1 (2026-08-21) proved the *logic*; `routing.test.ts` only asserted that route FILES existed, so nothing had ever executed a page. Round 2 built four surface suites that do: `ui-embedded.test.ts` (378 cases — real SSR render of all 16 `/app/*` pages through the web branch of the dual-surface seam, loaders + actions + free/plus + empty-data + foreign-shopId), `ui-web.test.ts` (225), `ui-admin.test.ts` (354 — every operator form POSTed through the real single-fetch endpoint and asserted to TAKE EFFECT, with byte-identical restore of every global setting), `storefront.test.ts` (227 — the whole 13-endpoint app-proxy contract with genuinely HMAC-signed requests), plus `features.test.ts` (199, module-by-module) and `polaris-events.test.ts`. `scripts/qa/TEST-CASES.md` grew to **278 numbered cases** (new areas R–V) and `scripts/qa/BROWSER-TEST-PLAN.md` adds 60 checks that only a human eye can run. **THE BIG ONE — 130 dead event handlers.** Every `<s-switch>`, `<s-select>`, `<s-checkbox>` and `<s-choice-list>` in the admin, web app and operator console carried an `onChange` that **could never fire**. React 18's `ChangeEventPlugin.extractEvents` only synthesises `onChange` for `<select>`, `<input type=file>`, text inputs and checkbox/radio (`shouldUseChangeEvent` / `isTextInputElement` / `shouldUseClickEvent`); a Polaris custom element matches none, `getTargetInstFunc` stays undefined and nothing is dispatched — and `change` is absent from `simpleEventPluginEvents`, so no other plugin picks it up. Verified in `node_modules/react-dom` source, not assumed. TypeScript could never catch it: `@shopify/polaris-types` declares `onChange?: (event: Event) => void` because that is the ELEMENT's API, not what React 18 can wire. Blast radius measured with a brace-depth-aware JSX scanner: s-select 38, s-switch 36, s-checkbox 31, s-choice-list 13, s-number/color/date-field 15, `s-banner onDismiss` 3 — **130 sites across 37 files**, i.e. Settings, AI Instructions, Chatbox, Availability, Proactive, and the admin Plans/Promo-codes/Logs pages all rendered correctly and did nothing. Fix: the Polaris manifest shows every affected control emits **both** `change` and `input`, and `input` IS in React's registry, so `onChange` → `onInput` is lossless — 127 sites converted. `dismiss` is not a React event at all, so the 3 banners now go through a new `app/components/ui/DismissibleBanner.tsx` (ref + `addEventListener`). `scripts/qa/polaris-events.test.ts` guards it permanently and tells you to delete itself on a React 19 upgrade. **D-39 closed** — the inbox filtered, searched and counted in JS over a `take: 300` window, so every rail tab showed only the slice of its category inside the newest 300 rows and the badge undercounted; a Plus shop is entitled to 1000 conversations/month, so this broke within weeks, not years. `listConversations(shopId, opts?)` now applies filter/search/unreadOnly in the DATABASE, new `getInboxCounts()` returns exact unbounded per-tab totals in one aggregate pass, and rail/search/unread moved into the URL so the loader is the single source of truth (search debounced 300ms, with optimistic narrowing only while the typed term EXTENDS the committed one — deleting characters widens the set, which no client filter can reconstruct). Independently verified against a 328-conversation fixture: all 7 tabs return rows from outside the old window, counts match an independent count, and the client `FILTERS[key].test` predicates agree ROW-FOR-ROW with the server `FILTER_WHERE`. **Other defects fixed this round:** `/app` and `/app/contacts` hard-500ed for any shop without a usable Shopify session because `access.getAdmin()` threw before the "fails soft" logic it was meant to protect — new `getAdminOptional()` degrades to `null` and the opportunistic Admin calls are skipped (the contacts ACTION resolved it eagerly too, so export/convert-lead/contact-save failed for want of a client none of them used). `analytics_range_days` was defined per plan (7/30/90/365) and read **nowhere** — a Free shop got 365 days for `?crange=12m`; new `clampRange()` is applied inside all six ranged reports (at the data source, not the loader, so a hand-crafted query string cannot bypass it) and reads the plan from the shop row rather than the 60s-cached config, because a gate that trails the cache is a gate a downgraded shop can still read through. `inbox_cart_view` was a display-only gate — `getConversationDetail` shipped the cart JSON to the browser on every plan and only the component hid it; the gate moved to the data source and strips **only** `cart`, since the same `pageContext` blob carries browsed-pages and device info that every plan is entitled to. `DISCOUNT_INTENT_RE` had no plural, so "any discounts today?", "do you have coupons" and "any offers" — the most natural phrasings — never injected the synced discount facts. Curated answers embedded `question + synonyms` as one blob, which diluted the vector so badly that an answer's own question scored **0.775**, under the 0.80 serve threshold: every synonym-bearing answer sat permanently in the borderline branch paying an extra LLM confirm call, and could be declined outright. Now the question is embedded alone (score **1.000**) and `curatedMatch` gained a second lane that matches synonyms as exact space-boundary phrases — which is what a synonym IS, the merchant stating "this wording means this answer". `scripts/qa/reembed-curated.ts` backfilled all 22 existing rows. Product Quiz was refused below Pro but SAVED on Pro/Plus even though `widget-renderer.js` has no quiz branch, so paying merchants configured a quiz and shoppers got a plain text bubble — now refused on every plan until a runtime exists. `stripToText` turned `<b>Rotterdam</b>.` into `"Rotterdam ."` and that spacing reached embeddings and the RAG context quoted to shoppers. `saveRuntimeConfig` merged onto a snapshot up to 30s stale, so saving one settings section **wiped the sealed Resend API key** — proven live. Five unlabelled inputs got accessible names. **Two PII leaks closed:** the email `log` provider printed the recipient AND the full body — including one-time invite/reset token URLs — and it is the DEFAULT provider, so production would have shipped credentials to stdout; and `email_send_failed` logged Resend's error body, which echoes the recipient, into `app_logs.context`, which the operator console reads CROSS-TENANT. The key-based denylist could not catch either, so `log.server.ts` now scrubs email-shaped substrings out of both the context and the `message` column. **In-app privacy disclosure was false** (App Store req 1.1.4): Settings → Privacy claimed "the only customer information ChatConvert stores is the email a shopper optionally provides", while the schema stores name, phone, coarse location and `shopifyCustomerId`, and it described uninstall as an immediate purge when there is a 7-day grace window. Rewritten to list every category actually stored, name OpenAI/Resend/17TRACK as processors, and state the real windows — this text is read verbatim by the data-protection reviewer. **Storefront:** a persistent visitor UUID was written to `localStorage` on every page load before any interaction; the session id is genuinely functional (spec 05 says so, and Shopify's Customer Privacy API gates analytics/marketing/preferences/sale-of-data, not service delivery) so it stays ungated but is now minted only at point of use — a shopper who never opens the chat leaves no identifier — while the `/event` analytics beacons ARE gated on `analyticsProcessingAllowed()` with a `visitorConsentCollected` listener and a settled/loading distinction so nothing outruns consent. Opening the panel left focus on `<body>` (the first "focusable" was the hidden Back button), so Escape never fired, the focus trap never engaged and Tab walked the merchant's storefront behind an open dialog. A `?ccprobe=1` debug SSE probe shipped to shoppers. Mobile `min-height` survived the 480px block so the composer could be pushed off-screen; now `dvh` + `env(safe-area-inset-bottom)`. **Shells:** the operator console had NO navigation below 900px (bare rail, hidden by the shared stylesheet, no topbar or drawer); the web app's mobile menu button and Inbox pill were white-on-white leftovers from a dark top bar — the menu button being the only way to open the nav on a phone, for the surface whose primary users are agents on phones. `TabPills` declared `role="tablist"` with no roving tabindex, arrow keys or `aria-controls`; keyboard contract implemented. `app/root.tsx` had no `ErrorBoundary`, so `/web/*` and `/admin/*` showed React Router's raw stack trace. Added `public/robots.txt` + `X-Robots-Tag: noindex` on `/web/*`, `/admin/*` and `/app/*`. **Gates:** typecheck 0, lint 0, build ✅, and every suite green except the two that need a running dev server. **Submission blockers that are NOT code and need the user:** protected customer data level 2 must be requested in the Partner Dashboard (fields: email, phone, name — NOT address; the app reads only `shippingAddress { phone }`); `shopify.app.toml` still points at a dev tunnel and the app-proxy URL is pinned per store at install time, so it cannot be corrected later without a real uninstall/reinstall; no privacy policy exists; `billingForceTestCharges` must be OFF after review; reviewer credentials must cover BOTH login surfaces; and the `[events]` TOML stub on `api_version = "unstable"` is untested on the deploy path. |
| 2026-08-26 | **Preview assets were pinned to the first build loaded in a browser session** (user: "message preview is showing [object object] in all the templates"). `ensureAssets` in both admin previews guarded injection with `if (!window.ChatConvertRenderer && !document.getElementById(SCRIPT_ID))`, so once a page had injected `widget-renderer.js` the global was never replaced — even though the loader re-reads the file from disk on every dev request. A session that loaded the app BEFORE the campaign-settings reshape therefore kept executing the old `campaignBubble`, which did `setText(msg, campaignText(campaign.message))` where `campaign.message` is now an object → `"[object Object]"`. Reproduced mechanically: the pre-change renderer fed the new shape prints it for **all 10 templates**; the current renderer prints it for none. Fix: new `app/lib/ui/widget-preview-assets.ts` stamps a content hash on the injected `<style>`/`<script>` and replaces anything stale (script removed, `window.ChatConvertRenderer` deleted, re-executed). **ChatboxPreview had the identical latent bug and was fixed in the same pass.** Defence in depth: `welcomeText`/`campaignHtml` now use a `typeof === "string"` guard instead of `String(x || "")`, so a future shape drift renders empty rather than painting `[object Object]` into a shopper's bubble. |
| 2026-08-26 | **Proactive chat rebuilt to the reference design** (user supplied `.claude/resources/proactive_chat/*.png` — 6 of the 10 templates; the other 4 were designed to match). The editor is now one screen for all ten templates: an **Activate proactive chat** card, then a two-column body — collapsible **General / Trigger / Conditions**, plus **Message** and **Appearance**, with a live **Message Preview** and a read-only summary card sticky on the right. Which controls appear is DECLARED PER TEMPLATE in `lib/campaigns/templates.ts` (`scopeMode`, `timingMode`, `showCartValue`, `messageKinds`, `showContentMode`), so the screen is data-driven rather than ten branches. **Settings shape replaced**: flat `pageTypes/message/ctaLabel/discountCode` → `{trigger, conditions, message, appearance}`; `migrateCampaignSettings()` lifts legacy blobs on read (verified idempotent, keeps the merchant's copy, escapes it into `<p>`), so no migration and no data loss. New capabilities: page scope (all/specific pages, all/specific product pages, all/specific collection pages), scroll-depth trigger, cart min/max value window, and the full Conditions set (audience, business hours, device, date window, countries). Message types per template: Text (Quick question → starter chips | Custom message), Product Recommendation (best sellers / new arrivals / similar / complementary / custom), Discount (code picker + usage instruction + Collect lead form), Smart Product Page floater (headline/subtitle/CTA with `{{ option }}`). **Product Quiz ships as a disabled `Pro +` tab exactly as in the reference** — the tab exists, the runtime does not; server-side it is refused. **Preview parity by construction** (same trick as ChatboxPreview): the admin injects the real `widget-renderer.js` + `chat-widget.css` and renders through `campaignBubble` — the SAME builder the storefront uses, so preview and shopper see identical DOM. Bubble colors are now **per campaign** (`--cwp-*` custom properties), not inherited from the chatbox theme. **Recommendation resolution split**: page-independent sources (best sellers / new arrivals / custom) resolve server-side into the cached widget config; contextual ones (similar / complementary) and the floater's variant chips resolve per page view through the new `proxy.campaign-products` — the 5-minute shared cache can't carry per-product data. Below Pro, `similar` **degrades to best sellers** instead of rendering an empty bubble. `proxy.campaign-lead` stores newsletter leads with the same contact-merge rules as pre-chat (never downgrades a customer; double opt-in withholds `marketingOptIn` until confirmed) and counts the submission as the campaign's click. Liquid now emits `data-product-id`/`data-collection-id`/`data-country`/`data-logged-in` for scope + condition matching. `remove_items` fires on an actual removal, detected by comparing the cart line count against the previous page view (per tab) — no theme hooks. **ATC attribution moved**: the reference bubbles hand off to chat rather than adding inline, so the bubble stamps `chatconvert_campaign` on the cart at hand-off and the in-chat add emits `campaign_atc` — revenue attribution survives, and the unreachable in-bubble ATC path was deleted rather than left as dead code. Verified: typecheck / lint / build green, `validate_component_codeblocks` ✅ VALID, evaluator suite **38/38** (rewritten for the new shape), metrics **8/8**, a 40-check round-trip against the dev DB (all 10 templates save, 4 plan gates refuse, 7 validation errors surface, XSS stripped, legacy migration, widget projection, contextual resolution, cross-tenant update refused), widget initial payload **18.11 KB gz** of 30 KB. |
| 2026-08-21 | **Public landing page rebuilt + app loading screen** (user supplied `.claude/resources/other/app-page.png` and `loading.html`). Landing (`app/routes/_index`): gradient hero panel with badge pill + headline + white action card, **six** feature cards in the reference tinted-icon-chip style (the reference had three; ChatConvert has more to ship), a CTA band pointing at `/web/login`, and a trust strip. Inline SVG icons, no new dependency. **Deliberate deviation:** the reference hero card is a "Shop domain → Log in" form; kept OUT because `_index/route.tsx` records "App Store review requirement 2.3.1 forbids asking a merchant to type their .myshopify.com domain here — do not re-add". NOTE: a shopify.dev search could NOT confirm that citation — the closest real rule is *Enable seamless sign up based on Shopify credentials* (Built for Shopify 3.1.3), which is about not requiring a **separate account** after install, not about a domain field, and Shopify own app template ships that form at `/auth/login`. The comment looks over-cautious; left as recorded pending a user call. Loading screen: `AppLoading.tsx` + `app-loading.css` (plain stylesheet via `links`, per the hydration-mismatch rule) mounted in `app/routes/app.tsx` for **both** surfaces. Two states, one mark: **boot** (opaque — server HTML is painted but React has not hydrated, so the page looks ready while nothing is clickable; `BOOT_MIN_MS` 400 + 260ms fade) and **nav** (same mark on a frosted veil so a page change never reads as an app restart; only after `NAV_DELAY_MS` 300 so short hops never flash). Deliberately **skipped for same-path loading** — the inbox rewrites `?c=` on every conversation click and revalidations fire constantly; a full-screen takeover there would be worse than nothing. `prefers-reduced-motion` honoured. Not applied to `/admin` (operator console) — say the word. |
| 2026-08-21 | **Pre-submission QA campaign — test cases written, executed, defects fixed.** Deliverables: `scripts/qa/TEST-CASES.md` (numbered cases per module with scenario variants) and `scripts/qa/test-matrix.xlsx` (118 feature rows × design/logic/tenancy/plan-gate columns + a defect sheet + a summary sheet; regenerate with `npx tsx scripts/qa/make-test-matrix.ts`, which uses the zero-dependency OOXML writer in `scripts/qa/xlsx.ts` — no xlsx library or Python on this machine, and `package.json` stays clean). **Three decisions taken by the user:** flip plan enforcement to `enforced` and leave it on; build the gates/quotas the plan spreadsheet specifies but the code lacked; fix defects as found. **Plan matrix is now FINAL** — `DEFAULT_PLANS` reconciled against `.claude/imp-details/plan-allocation.xlsx` (manual_qas 10/25/50/100, policy_pages 5/10/15/20, crawl_pages 1/10/15/20, team_seats 1/2/5/10 corrected) and `DEFAULT_ENFORCEMENT` flipped `"open"` → `"enforced"`, so every quota and gate is live. **Six new seams built** (identifiers in `plan-shared.ts`, one server-side enforcement point each): gates `survey` (Basic+, `proxy.survey`), `push_notifications` (Basic+, `app.push-subscription`), `custom_recommendations` (Pro+, `instructions/save.server`), `multi_language` (Plus-only, `instructions/save.server` — found ungated during planning, not in the user's original list); quotas `active_campaigns` (0/2/10/unlimited, `campaigns.server`) and `analytics_range_days` (7/30/90/365, `reports.server`). Spec-15 downgrade rule honoured throughout: over-quota data is kept, only new creates are blocked, and the OFF→ON transition is what's gated (a downgraded shop can still save the setting it already had). **43 defects found, 36 fixed, 4 open, 3 documented** — full table on the Defects sheet. Highest-impact: `agentOnline` was never wired so two of three availability modes were dead; the admin temperature override silently de-tuned the strict-JSON router for every tenant (reproduced live — truncated JSON, every turn falling back); `shop/redact` left billing fields intact so a reinstall inside the grace window resurrected a paid plan against a cancelled subscription; `web.logout` and `web.forgot` had no CSRF guard; the promo-code normaliser was a guaranteed no-op; promo redemptions were never confirmed when the merchant closed the tab; `FROZEN` subscriptions kept the paid tier; the operator console was framable by appending `?shop=`; and the template's `.myshopify.com` login form was still shipped (App Store req 2.3.1). **Still open (deliberately, all logged):** inbox filters + unread badge are computed in JS over a 300-row window and undercount above that (correctness, not just perf); cold 12-month analytics issues 2,191 statements; a LEFT JOIN in knowledge search prevents HNSW from ever being chosen; `migrate:new` scrubs the wrong folder when a future-timestamped migration exists. **Perf work:** measured at 20k conversations / 200k messages — four hot paths were seq-scanning and contact detail took 2.6s; two migrations added (`perf_hotpath_indexes`, `analytics_payload_gin`) taking inbox list 73→28ms, rollup 123→8.8ms, top questions 147→7.4ms, contact detail 2627→1.0ms; synthetic volume fully removed afterwards and the DB verified back to baseline. **New suites** in `scripts/qa/`: plan-gates, promo-codes, subscription-webhook, availability, handover, logs, model-portability, routing, auth-sessions, cache, perf-queries, install-lifecycle, plus `seed-curated` (20 curated answers incl. deliberate near-miss pairs for the 0.80/0.65 thresholds). |
| 2026-08-21 | **Turn trace / Test AI inspector** — user asked where to watch the LLM actually work: how it reaches the DB, what it retrieves, how it decides. Answer taken: **Test AI was the right surface but showed almost nothing** (sourceLayer + router intent + card titles), and `PipelineFrame` already declared a `debug` frame that was never yielded anywhere. Built: `app/lib/pipeline/trace.server.ts` (collector; noop by default so the storefront path pays nothing, never throws, bounded at 80 steps / 400-char strings / 12-item arrays) + `trace-types.ts` (client-safe shapes so the console can type the frame without importing a `.server` module). `runPipeline` takes an optional trace and records ~20 decision points: config + live thresholds, gatekeeping, handover triggers, all three guardrail layers, the embedding, curated match (score vs serve/borderline + the yes/no confirm call), recommendation match, history, router JSON, moderation, lane choice, hybrid search (every candidate with vector score / fused rank / keyword coverage / matched terms), the relevance-cut rule, the **exact allow-list handed to the model**, cross-sell, RAG hits with scores and excerpts, the store-info block, and the saved reply. `debug` frame replaced by a typed `trace` frame yielded behind `done` **by api.test-chat.tsx only** — proxy.chat.tsx passes no trace. UI: `TurnInspector` (timeline, per-step fold, copy-JSON) below the console, replacing the cramped inline "Review source" popover; `PipelineFlowGuide` ("How a reply is decided", 11 stages quoting this shop live thresholds from the loader — no duplicated constants). CLI companion `npm run trace -- "message" [--shop domain] [--json]`. **Nothing is persisted** — the trace dies with the page; `Message.sourceLayer/intent/productCards` stays the durable record, so no migration. First live run found a real accuracy bug: "keep my hands warm under 30 dollars" ranked **Scented Soy Candle** joint-top with the gloves because the stemmer matches "hands" to "**hand**-poured", giving it the same keyword coverage (2) and so the same relevance tier. Not fixed — logged for a stemming/false-positive pass. |
| 2026-08-21 | **Billing flow fixes + promo codes.** Three real-store defects found while testing plan changes on the dev store: ① the approval page never appeared because `/admin/settings` had **Billing test mode** on (mock provider) — it is now off; "Force test charges" is only needed in production for reviewer/partner stores since `test:true` is automatic outside production. ② `returnUrl` pointed at the raw app domain, so after approval the merchant landed top-level outside the admin — `callbackUrl()` now builds `admin.shopify.com/store/{store}/apps/{api-key}/app/billing-callback…` (same default `@shopify/shopify-api` uses), so the callback arrives embedded and the redirect helper bounces inside the admin. ③ the anti-replay check compared the GID `gid://shopify/AppSubscription/123` with Shopify's bare numeric `?charge_id=123` → always "Subscription not completed" (plan still changed via the `app_subscriptions/update` webhook); now compares the trailing id. **Promo codes** (user request): the discount card was a cosmetic stub that said "applied" to any string — replaced with a real system: `PromoCode`/`PromoRedemption` tables (migration `20260821120000_promo_codes`), `/admin/promo-codes` console page (Coupons: percent or fixed, duration, plan/interval scope, max uses, expiry, activate/deactivate, delete only if never redeemed), `validate_code` + code-aware `subscribe` actions on Plan & Usage with discounted-price preview on the cards, and the discount passed to Shopify on the recurring line of `appSubscriptionCreate` (Shopify applies it — approval page + invoices). Shopify has no coupon concept of its own; this is the only way to do merchant-entered codes. Plan cards restyled (bigger title/price, ✓ bullets, accent border only on the current plan, segmented Monthly/Yearly pill with the savings chip inside). |
| 2026-08-21 | **Logging & observability (spec 21)** — user asked whether the app needs a log system or analytics, and whether logs should be merchant- or company-specific. Answer taken: **analytics was already built** (spec 14 merchant-facing + spec 19 admin token usage); the real gap was **operational logging**, and it is **company-specific only**. Before this the only diagnostic was 81 raw `console.error`/`console.warn` calls whose output died with the host's stdout — a merchant reporting "the widget stopped answering yesterday" was unanswerable. Decisions (user): ① **build it inside `/admin`**, not on a hosted log service — the operator console already had DB auth, a cross-tenant page pattern and a nightly purge job, so a log table reuses all three at zero cost and zero external dependency (works in dev and offline); Sentry's free tier stays a later option for uncaught crashes. ② **Save the specific record only** — errors and warnings, never a row per request; that ceiling (plus a 14-day purge, a 50/hour per-event rate cap, and denylist PII redaction) is the entire reason this can live in Postgres alongside pgvector without becoming a cost centre. ③ **No merchant-facing activity/audit log** — chat apps don't ship one and nobody reads it; revisit only when spec-18 team accounts make "who changed what" genuinely ambiguous between multiple humans. Trade-off accepted: self-hosted logging cannot record failures where the app or DB is the broken thing — covered by the seam mirroring every call to console, which the host's own log viewer still captures. Churned merchants' error history is deleted with them (same trade as `llm_usage_daily`). |
| 2026-08-20 | **Admin UI round 2** (user list): ① **rail switched dark → light** (`#fff` + `#e5e5ea` border, nav ink `#4a4a53`, hover `#f4f3f8`, active `#efe8ff`/`#5b2fd0`, avatar tinted, mobile top bar too) — edited in `web-shell.css`, so **the standalone web app changes with it** (kept deliberately shared; say the word to fork them). ② **Admins page = list + "Add new admin" button**; adding and password change now happen in `s-modal` popups, password reachable via a per-row edit icon on your own row, remove via icon + confirm. ③ **Sign-out confirms first** (`ConfirmDeleteModal` in `AdminShell`, form submits only on confirm). ④ **Save feedback = toasts, not top-of-page banners** — every admin fetcher now calls `useAppBridge().toast.show()` (the `SurfaceProvider surface="web"` shim renders them bottom-centre, same as the web app); all `s-banner` result banners removed (contextual warnings like "enforcement is off" stay). ⑤ **Tier meter recoloured**: a lighter single-hue ramp was tried and **rejected by the dataviz validator** — 4 light steps of one hue can't clear the adjacent normal-vision ΔE≥15 gate (best 13.3), so the meter now uses the first four *categorical* slots (blue/orange/aqua/yellow, worst adjacent ΔE 22.9 normal · 9.1 CVD, ALL CHECKS PASS) which are both lighter and properly distinguishable. ⑥ **Sub-cent costs readable**: `formatUsd` now shows 6 decimals below $0.0001 instead of rounding to "$0" (real amounts at dev token volumes were displaying as zero and reading as "tracking is broken"). Verified: typecheck/lint/build clean, all 6 admin routes 200, admins page renders both modals with zero `s-banner`, built CSS contains no `#14141a`. |
| 2026-08-20 | **`s-page` needs an `s-stack` wrapper** (user: level-1 card resolved `margin-block-start: var(--s-section-margin-block-start-NNNNN, 0rem)` to the 0rem fallback → sections stacked flush). Polaris sets that per-instance variable from layout context; sections placed as DIRECT children of `<s-page>` never get it. Both existing admin pages (`app._index`, `app.plan-usage`) wrap their whole body in a single `<s-stack gap="base">` — that stack is what spaces the cards. All 7 admin routes rewrapped to match. **Rule for new pages: `<s-page>` → one `<s-stack gap="base">` → sections.** Rule added to the polaris-admin-ui skill. |
| 2026-08-20 | **Admin console realigned to the app's design system** (user: "admin ui is not similar to the app or web app ui — not a good practice"). The user was right: the 2026-08-20 "make it beautiful" pass had built a THIRD look (bespoke `ccpf-card`/`ccpf-table`/`ccpf-tile`/`ccpf-seg` + its own dark sidebar with different tokens), violating the standing rule to use Polaris/design-system components wherever one exists. Fix: the operator console is now a **sibling of the standalone web app, not its own product** — `AdminShell` reuses the web shell's markup and stylesheet **verbatim** (`ccws-*` from `web-shell.css`: same rail, brand mark, nav treatment, active state, content surface), and every page body is Polaris `<s-page>`/`<s-section>`/`<s-table>` + shared `app/components/ui` (`StatTile`/`StatGrid`, `TabPills`, `PageHeader`, `EmptyState`) — the same components the merchant pages use. `admin.css` shrank from ~640 lines of bespoke chrome to ~140 lines of genuinely-unique visuals only (plan-distribution meter, daily-cost bars, share bars), all expressed in Polaris CSS vars. Note the surfaces stay ChatConvert-branded rather than Shopify-admin lookalikes — that's the spec-18 Polaris-licence decision, so "consistent" means matching the web app, which is exactly what this does. Verified: typecheck/lint/build clean; all 7 admin routes 200 with `ccws-rail`/`ccws-navItem`/`ccws-content` + `s-page` in the rendered markup. |
| 2026-08-20 | **`dev:stop` was leaking a dev server on every cycle** (user: "admin page is not opening" — the code was fine; the running server was stale). Root cause: `shopify app dev` binds the ports, but the real app server is a CHILD `react-router dev` (Vite) process that listens on NONE of them, and Windows does not kill children with their parent — so the port-only `dev:stop` orphaned one Vite server per stop/start. **Eight had stacked up across one day** (10:50→17:02); they held the Prisma query-engine file lock (the EPERM that broke `prisma generate` twice) and kept stale Prisma clients in memory, so pages failed against a client that predated the `llm_usage_daily` migration. Fix: both scripts moved out of package.json JSON-escaping into `scripts/dev-stop.ps1` + `scripts/dev-tunnel.ps1`. `dev:stop` now also matches project dev servers **by command line** (`chat-convert` + `@react-router`/`vite`, so unrelated Node apps and the Shopify MCP server are untouched), uses `taskkill /T` for tree kills, cleans orphaned `query-engine-windows` processes, and **reports anything that survived** instead of exiting silently. `dev:tunnel` now runs stop → `prisma generate` (engine unlocked, guarantees client matches schema) → CLI with the tunnel URL. Verified live: killed the CLI + both orphans, all dev ports free, zero project node processes left, `prisma generate` clean. |
| 2026-08-20 | **Env → admin settings migration** (user: "do we need the llm variables in env … which can be set as settings in the admin"): audited every env var and moved all *operational* config to a new `/admin/settings` page backed by `app_secrets["admin:runtime"]`. **Stays env** (no dashboard equivalent): DATABASE_URL, the SHOPIFY_* identity group, ADMIN_ADMIN_* (bootstrap chicken-and-egg), LLM_PROVIDER (single impl), **EMBEDDING_MODEL (deliberate — 1536-dim vectors need a re-embed migration, not a toggle)**, PORT/NODE_ENV/PRISMA_CLIENT_ENGINE_TYPE. **Now dashboard-managed with env as fallback**: OPENAI_API_KEY, CHAT_MODEL, EMAIL_PROVIDER/EMAIL_FROM/RESEND_API_KEY/SMTP_*, WEB_APP_URL, SHOPIFY_APP_STORE_HANDLE, BILLING_TEST_MODE, BILLING_FORCE_TEST_CHARGES, EMBED_STATUS_ENABLED. Design: `runtime-config.server.ts` — precedence dashboard→env→default, **sync** accessors (30s TTL + eager boot load) so no consumer signature changed, never throws; secrets (OpenAI/Resend/SMTP password) **AES-256-GCM encrypted at rest** (`secrets-crypto.server.ts`, key derived from SHOPIFY_API_SECRET), write-only in the UI, never sent to the browser. Consumers rewired: OpenAI client rebuilds on key rotation (no restart), email transport re-created on credential change, billing test flags, embed-status, webBaseUrl, ingestion key checks, review link. Page tools: Test OpenAI connection, Send test email, per-section save, Clear-all→env. Verified: `scripts/admin-settings-check.ts` **25/25** (persistence, dashboard-beats-env, encrypted-at-rest with plaintext absent from the row, each consumer, partial saves preserve other fields, reset→env), all admin pages 200 / unauth 302, a real save through the action landed in the DB, and a **cross-site POST was rejected with no write**. typecheck/lint/build green; admin-check, usage-check, smoke all PASS. |
| 2026-08-20 | **Per-merchant token usage analytics** (user: "analytics for each shop … how much tokens are used by particular merchants"): new `/admin/usage` + `/admin/usage/:shopId`. Decisions (user): track **chat AND embeddings** (ingestion is a large share of cost for big catalogs); **no manual price entry** — price table maintained in code (`app/lib/admin/llm-pricing.ts`, figures from OpenAI's official pricing page, `PRICING_VERIFIED_AT` shown in the UI, unknown models flagged "unpriced" rather than costing $0); merchant table + per-shop drill-in. Architecture: `LlmCallContext { shopId, purpose }` made a **required** argument on every `LlmProvider` method so the compiler enumerated all 26 call sites (no silent under-counting); exact counts from the API — `stream_options: { include_usage: true }` makes even STREAMED replies report usage (verified live); `llm_usage_daily` rollup (shop × UTC day × model × purpose, migration `20260820103900_llm_usage_daily`) written **fire-and-forget** (never awaited, never throws, blank shopId dropped). Cost = (prompt−cached)×input + cached×cachedInput + completion×output. Uninstall cleanup deletes the table too, so the audited zero-row contract holds (verify-compliance now 31 tables) — trade-off: churned merchants' cost history goes with them. Verified: `scripts/usage-check.ts` 13/13 **live against OpenAI** (streaming + non-streaming + embeddings + report + price math), HTTP walk on the built server (index/range/detail 200, bogus shop 404, unauth 302), verify-compliance ALL PASS, typecheck/lint/build green. **Tracking starts at deploy — there is no historical backfill.** |
| 2026-08-20 | **Full manual-QA sweep + fix pass** (user: "test every feature … not a single error or misfunctional"). 6 tester passes (storefront widget/proxy · AI-agent admin/knowledge/curated · inbox/contacts/push · chatbox/settings/dashboard/analytics/campaigns · billing/compliance/sync/webhooks · web-app/admin/mobile) → ~75 verified defects, all fixed. **Highest impact:** blocked visitors could still chat (pipeline now refuses + widget locks composer, new `visitor_blocked` outcome); merchant replies never reached shoppers outside human-mode (widget polls whenever a conversation exists, with 5s→15s→30s idle backoff); unread badges counted Test-AI chats (SSE said 276 vs list 0); no handover email until the owner first opened the web app (owner row bootstrapped lazily in `notificationRecipients`); yearly subscriptions were rejected by Shopify (usage line omitted on ANNUAL — yearly hard-caps); a plan rename broke subscription verification (name from `DEFAULT_PLANS`, matched against both); `customers/redact` with no email redacted EVERY DataRequest (Prisma dropped the `undefined` filter); stale/out-of-order `app_subscriptions/update` downgraded paying shops; reconcile never pruned deleted products/collections; `proxy.prechat` was the only widget route not session-binding `conversationId` (cross-session contact hijack); `/order-track` had no throttle and accepted a 2-char order suffix + 7-digit phone tail (now 8/min per shop+IP, full-number matching); a truncated SSE stream hung the widget forever (settled-flag + 45s watchdog); FAQ search was dead for shops with no *featured* FAQ. **Also:** 30-min metering rule was dead code (compared a timestamp to itself — `previousLastMessageAt` threaded through `tickConversation`); CSV formula injection in contact export; case-sensitive email matching made duplicate contacts; FAQ CSV header heuristic ate real rows (exact tokens + dedupe + `headerSkipped`); published FAQs could be saved with an empty answer; .pdf/.docx uploads created quota-consuming error rows; SaveBar had no leave-guard on the web surface; web `admin` could hijack a peer admin via `team-resend`; login lockout enabled account enumeration; last-admin guard was a non-atomic count-then-delete. **Router guard (new, code not prompt):** `routed.blocked` is honoured only when the merchant actually configured banned topics — with an empty list the model was inventing a policy and refusing shoppers; moderation + keywordScan + meaningScan are unaffected. **Known-unfixed:** "something that blocks rfid" still rolls `blocked` ~1/3 on the dev shop because the router over-matches the configured list (likely "weapons"); a real fix needs router-prompt tuning (FROZEN — needs user decision). Corroboration-by-meaningScan was evaluated and REJECTED: meaningScan does not catch "medical advice" at the configured threshold, so it would weaken real guardrails. **Gates:** typecheck 0 · lint 0 · build ✅ · golden 16/16 ✅ · smoke ✅ · billing-mock ✅ · verify-compliance ✅ · analytics ✅ · campaign metrics + triggers ✅ · admin-check ✅ · widget 14.29KB gz (≤30). |
| 2026-08-20 | **Admin console redesign** (user: "design is very basic … make it beautiful"): /admin rebuilt as a custom-branded operator console — dark sidebar (#131021 + purple radial glow, gradient active nav pill, inline-SVG icons, signed-in chip with avatar + icon sign-out), light content on #f5f4fa with 16px-radius soft-shadow cards, gradient eyebrow page headers, stat tiles (tinted icon chips), dot-status pills, styled tables with letter avatars, segmented tier switcher on Plans, feature checkboxes in bordered choice cards, kv summary strip on the AI page, Admins as avatar rows + 2-col form cards. All presentation-only — loaders/actions untouched; Polaris `s-*` retained for inputs/switches/buttons/banners; plain stylesheet via route links (no inline styles); <=960px sidebar collapses to top bar (icon nav). Plan-distribution meter = ordinal single-hue ramp free→plus (#d9c9fa→#47209e, dataviz skill: 2px segment gaps, counts in legend, identity by fixed order + labels not color alone). Custom tables/buttons in the shell are deliberate (operator surface, not merchant admin — same policy as the web shell). |
| 2026-08-20 | **Admin panel (spec 19)**: user wants a company-side dashboard whose settings apply to all merchants; v1 = AI model settings + plan features. Decisions (user, via plan-mode Q&A): **DB-backed admin accounts** (not env creds) — `platform_admins` (scrypt, spec-18 helpers) + `platform_sessions` (hashed opaque token, cookie `cc_admin`, 7d sliding; Path=/ — a /admin-scoped cookie never reaches the `/admin.data` single-fetch URL per RFC 6265 path rules, which dead-ended the Overview tab until fixed 2026-08-20); AI settings = chat model picker **plus** optional global temperature/max-tokens overrides that WIN over per-call tuned values when set (blank = keep tuning; UI warns re golden set); enforcement switch lives on /admin/plans and **supersedes the "edit ENFORCEMENT const" step** (pending-manual item 10 updated). Architecture: overrides in `app_secrets` (`admin:ai`, `admin:plans`); `PLANS` export kept and **mutated in place** on load (module-init + 30s TTL + post-save reload) so all existing consumers pick up changes without signature churn; embedding model deliberately NOT editable (vectors pinned to 1536 dims). Admin routes are cross-tenant BY DESIGN — tenancy rule there = requireAdminUser in every loader/action + no shopId from user input (spec §Tenancy exemption). |
| 2026-08-20 | **Mobile responsiveness (spec 20)**: user wants the whole app usable from a phone (web surface is mobile-first for Agents) with desktop unchanged. Decisions: one 768px breakpoint; additive media-gated CSS only (inline-styled layouts get opt-in classes or `useIsMobile()` attribute swaps — s-grid templates cannot be overridden from external CSS); web nav on phones = top bar + slide-in drawer (user choice over bottom tabs); Inbox mobile = URL-driven pane switch on ?c= (list <-> thread) + details slide-over reusing InboxDetails wherever the column is hidden (<1241px); tables pan horizontally inside DataTable rather than reflowing to cards; touch reordering = visible up/down buttons next to drag handles on coarse pointers. |
| 2026-08-19 | **Global date & time format** (Settings → General → Store information): `storeInfo.dateFormat` (7 regional formats incl. ISO and 年月日) + `storeInfo.timeFormat` (12h/24h), shown in the store time zone (Shop.timezone from Chat availability). Single formatter `app/lib/format/datetime.ts` (+ `DateTimeProvider`/`useDateTime()` in app.tsx, `getDateTimePrefs()` server-side) replaces every `toLocale*` call in admin/web UI — fixes the locale/TZ hydration mismatches for good and gives merchants worldwide their own notation. Analytics date-only series stay UTC-day based. |
| 2026-08-19 | **Standalone web app + team logins + browser push (spec 18)**: user asked for Chatty-style "Open in web" so non-Shopify-staff teammates can review chats and get web notifications. Decisions: (1) **same /app URLs, dual auth** — `requireShopAccess()` replaces `authenticate.admin()` in every /app route: Shopify signals → admin path unchanged; opaque HttpOnly `cc_web_session` cookie → `TeamSession` row (4th trusted shop-identity source, SameSite=Lax so it never reaches the admin iframe); (2) whole app on web, **billing mutations admin-only** (Plan & Usage read-only + admin deep link); (3) roles owner/admin = all, **agent = Inbox + Contacts**; (4) email = **Resend** via fetch behind `EMAIL_PROVIDER` (log fallback + copy-link UX, so invites work without a key); (5) push events default = handover + shopper replies in human-mode chats (per-member toggles, new-conversation opt-in), native Web Push/VAPID (no vendor), payload encrypted e2e; (6) roster moved from `ShopSettings.team.members` JSON to `TeamMember` table (ids preserved; `assigneeId="owner"` literal kept); (7) new plan quota `team_seats` (1/3/5/10 provisional, open enforcement); (8) Polaris `<s-*>` reused on web via `<AppProvider embedded={false}>` + `useAppBridge` shim; web shell is ChatConvert-branded (Polaris licence: visually distinct from admin). App Store: embedded stays primary (2.2.2); reviewers need a web test login (4.5) → pending-manual. |
| 2026-08-19 | **Manage metafields shipped** (Training data → Products; reference `resources/other/productMetafields.png`, Order tab excluded per user): new `product_metafield_definitions` table + `Product.metafields` (all synced product/variant metafields) + `Product.metafieldText` (rendered ENABLED ones → embedding text, tsvector weight C, lexicon, LLM snippet). Modal: Product / Variant tabs, Sync now (definitions + used-in), search, Enabled/Disabled select, supported-types list, plan cap `metafields_enabled` (free 3 / basic 10 / pro 25 / plus 100, open enforcement). Toggle → `metafield-apply` job re-renders + re-embeds only changed products (verified: enable Ingredients → 7 products re-embedded, keyword search hits metafield text; disable → 7 back). Webhook product upserts refetch metafields (payload has none). Migration `20260819120000_product_metafields` rebuilds the generated `searchText`. **Dev server must be restarted** for the new Prisma client + job handler. |
| 2026-08-19 | **Metafields: structured only** (user decision, after review of the demo store: 56 defined vs 17 legacy `namespace.key` app metafields — judgeme/global/mc-facebook/seoconvert noise): the catalog lists only Shopify metafield definitions and the product query keeps only values with `definition { id }`; legacy rows are dropped on sync (verified: 73 → 57 rows, stored keys = 5 defined ones). Modal Status is an `s-switch` + All/Enabled/Disabled filter beside the search. Merchants promote legacy metafields via Shopify Settings → Custom data → add definition. |
| 2026-08-19 | **Metafields freshness** (user: 1+2+3, no toggle): `SyncState.metafieldSyncAt` + "Last synced" in the modal; `metafield_definitions/create|update|delete` webhooks (new route `webhooks.metafield-definitions.tsx`, enqueue-only → `metafield-definitions-sync` job = re-mirror catalog + re-render product text if an enabled definition vanished); note "Auto sync: follows Products auto sync and Shopify webhooks". **New scope `read_content`** (required by those topics — see review-checklist §4); dev store must re-authorize on next `npm run dev` load, `npm run deploy` for production. Migration `20260819150000_metafield_sync_at`. |
| 2026-08-19 | **Metafields modal polish** (user): unsupported types hidden from the list (footnote "N metafields aren't listed…"), footer link → second popup "Supported metafield types"; `color` now supported (hex → nearest colour name, e.g. "red (#ff0000)"); modal locked to 85vh (`BrowseModalShell fixedHeight`), "Last synced: …" inline on the heading row (`headerMeta`), Sync now primary + right of the owner tabs, no explainer box. Backlog: metaobject / product-reference support (needs `read_metaobjects`, ~2–3 days) — the real "unsupported" payoff. |
| 2026-08-18 | **Phrase matches gated by information gain** (user: zodiac Cancer returned only Moonstone; Shopify search also found Strawberry Quartz): the adjacent-word phrases added 2026-08-18 can be accidental adjacencies ("sign is Cancer" → "sign cancer") and were splitting equally-relevant products into different coverage tiers. A phrase now counts (weight 2) only if its rarest word matches ≥ 4 products and the pair ≤ half of them (one df scan per buy turn); otherwise weight 0. Verified live: zodiac Cancer → Strawberry Quartz + Moonstone (2 cards); ruling number 8 → still the 2 correct products. Note: bare "my zodiac sign is Cancer" (no product word) is routed to chat by the frozen router — needs a product word to search. |
| 2026-08-18 | **Compact product replies** (user: text repeated title/price already shown on cards): `PRODUCT_RECOMMEND` prompt tuned (frozen-prompt tuning event) — no product names/prices in the text, 1-2 sentences saying WHY the picks fit (using matched details) + offer to help; buy-lane maxTokens 220 → 90. Golden asserts for product cases moved from reply-text regex to card titles. `npm run eval:golden` **16/16 PASS**. Also: message-tier search now keeps numbers (except the budget), corrects typos against a per-shop raw-word lexicon (trigram, cached 10 min), and counts adjacent shopper words as phrases ("ruling number", "number 8") at router weight — traced live: "my rulling number is 8…" → exactly the 2 ruling-number-8 bracelets. |
| 2026-08-18 | **Relevance cut for product cards** (user: "do you have black bracelets" always returned 4 cards just to fill the count): `selectRelevant()` in product-search keeps only the top match tier — every candidate with the top coverage (keyword tier), or vector-only rows within 0.04 of the best score, or the whole browse/hand-picked pool — min 1, max 4; the LLM allow-list = the SAME set so text and cards agree. Golden: "gloves I can use with my phone" → 1 card, "bottle that keeps drinks hot" → 1 card (tumbler), "the waterproof one?" → 1 card, browse fallback still 4. Known router flake (not new, prompts frozen): "something that blocks rfid" on the demo persona scope came back buy / off_topic / router-blocked across three temp-0 runs of gpt-4o-mini — the case stays in the golden set as a signal; 15/16 or 16/16 depending on that roll. |
| 2026-08-17 | **Chat model trial: gpt-4.1-mini rejected** (user asked to try it): with the frozen router prompt, `CHAT_MODEL=gpt-4.1-mini` scored 15/16 twice — it routes the follow-up "the waterproof one?" (after jackets / under $100) to `question` instead of `buy`, and its keywords for "keep my hands warm under $30" push the gloves to card #4. gpt-4o-mini stays 16/16 → keep `gpt-4o-mini` in production; revisit only with a router-prompt re-tune + golden re-run. |
| 2026-08-17 | **Search ranking: coverage-first** (user: "my birth month is february, need birthstone bracelet" returned generic bracelets; live trace on jgw-check showed router keywords = [birthstone, bracelet] and ts_rank burying the only February product below 40 bracelets; all 76 products also still had OLD embeddings). Fixes: ① keyword lane orders by **coverage** = weighted count of distinct query words matched (router ×2, shopper words ×1), then kwHit, then ts_rank_cd; ② fusion sorts by coverage first, RRF only breaks ties (vector-only rows = coverage 0 → mirrors demo keyword-first behaviour); ③ per-candidate `matchedTerms` returned and appended to the LLM snippet ("matches: birthstone, bracelet, month, february"); ④ `ts_headline` now uses the shopper-word query so the fragment shows the discriminating attribute; ⑤ new `scripts/reembed-products.ts` re-embeds stale rows straight from the DB (ran it: dev-shop 40, jgw-check 76). Result: Amethyst February bracelet ranks #1 (cov 6) above cov-5 bracelets. |
| 2026-08-17 | **Catalog auto-sync toggle** (user request, mirrors the Discounts real-time switch): Products and Collections "Manage data" rows get an **Auto sync** switch with the last-updated line directly beneath. Semantics per user: the toggle governs ONLY the daily full re-sync (`reconcileAll` now checks `catalogAutoSyncAllowed(shopId, type)` per shop/type); Shopify webhooks always apply immediately; manual Sync always works. Stored in `ShopSettings.catalogAutoSync.{products,collections}` (default on); new plan feature `catalog_auto_sync` on Pro + Plus (open enforcement today → visible everywhere; when enforced the switch shows a Pro badge + Upgrade link and is disabled). Action intent `catalog-autosync` (requirePlan seam). |
| 2026-08-17 | **AI Agent pages Polaris redesign** (user: "design is very basic and spaces are incorrect; use Polaris components where they fit"; Chrome extension was not connected so this was a code-level pass — visual check by user pending): all 5 AI-agent routes keep back + actions INSIDE the page (user reverted a title-bar-slots attempt the same day): sub-views via `PageHeader` (back button + tabs + toolbar), home status/actions row as `s-grid` + `s-button-group`; home = `s-banner` status banners, unresolved card as `s-grid`, setup steps as `s-grid` of bordered `s-box` cards with `ProgressTrack`, promo card; LearnCard/Manage-data rows → `s-grid`/`s-stack`/`s-button-group` with sync badge; Products view modal rows → `s-grid` + `s-divider`; General instructions → sections wrapped in `s-stack`, style presets as `s-clickable-chip`, helper copy moved into field `details`, language section merged (select + auto-detect); Handover → native radios/custom RadioRow replaced by `s-choice-list` + config `s-box`, trigger rows as `TriggerRow` grid with `s-divider`, rule form in subdued box; Recommendations → titles/actions Polaris text/stacks, row click opens editor, cross-sell pairs as grid rows, step hints as `s-banner`; Knowledge → AddTile as `s-clickable`, tiles grid as `s-grid`, empty state via `EmptyState`; Review → `EmptyState`, actions as inline `s-stack`. Rule added to polaris-admin-ui skill ("Page shell"). typecheck/lint clean. |
| 2026-08-17 | **Chat accuracy batch — Python-demo parity + retrieval upgrade** (user: app answers less accurately than `chatconvert_ui.py`, esp. when the ask lives in a product description). Root causes found by line-by-line comparison and fixed: ① keyword lane was AND (`plainto_tsquery(all keywords)`) vs demo ANY — now OR per router keyword + a 0.5-weighted tier of the shopper's own words, weighted tsvector title(A)/type+vendor+tags(B)/**full** description(C) (migration `20260817120000_product_search_weighted`, `immutable_array_to_string` helper), `ts_headline` fragment per hit, keyword ⊕ vector merged by reciprocal rank fusion (k=60) → cards = top-4 by relevance; ② product embedding text = title. type. vendor. tags. full description (`productEmbeddingText`, shared with seed; contentHash over it ⇒ every product re-embeds on next sync); missing OPENAI key at sync now warns + `embedding_skipped` event; ③ LLM candidate payload `{title, price, snippet}` (snippet = type · tags · matching description fragment) — prompt TEXT unchanged, input format changed; ④ router injected `STORE SCOPE: this store's products and services` / `BANNED TOPICS: (none)` when empty → over-flagged off_topic; now appended only when configured (demo behaviour); ⑤ current shopper message was in the history window AND appended again (sent twice every call) — `loadHistory(..., {excludeMessageId})`; generation window 6 → 10 (= router, = demo); ⑥ buy-lane generation 0.4/160 → demo 0.3/220; ⑦ handover defaults tripped in normal testing and silenced the AI (`mode: human` via `aiWhileWaiting: outside_hours` while online): repeatedQuestion 2→3, cannotAnswer 2→3, aiWhileWaiting → `always` (human reply still takes over), explicit-ask regexes need an intent verb (bare "customer service" = question). Golden set extended to 16 checks (description-level asks with card-title assertions, customer-service question, 3-turn continuity, repeat ×2 no-handover, history-window sanity) — `npm run eval:golden` **16/16 PASS** live; `npm run smoke` PASS; typecheck/lint clean. Rollout: run "Sync products" once per shop (or wait for daily reconcile) so real catalogs re-embed with the new text; shops with a saved handover row keep their thresholds. Backlog: per-chunk product vectors for very long descriptions. |
| 2026-08-12 | **Master learn switches decoupled from rows** (user-reported bug: toggling one row flipped the top switch, because it was computed as all-rows-on): Learn card switches on Products/Collections/Discounts now store an independent shop-level permission `ShopSettings.learn.{products,collections,discounts}` (new `learn-master` intent); per-row/bulk `learnEnabled` is subordinate (applies only when master ON). Runtime gates: buy lane skips catalog search+browse when `learn.products` off; question lane skips discount context when `learn.discounts` off; collections flag stored for future consumers. Dead `*-learn-all` intents removed. |
| 2026-08-12 | **Selection-bar overlay + contacts export menu** (user request): DataTable's "N selected" bar now OVERLAYS the table header (absolute position, Polaris IndexTable behavior) instead of inserting a row — no layout shift on select; bar includes the select-all checkbox since it covers the header's, plus a "Select all N" button (all filtered rows across pages — replaces the removed learn-all path for whole-catalog bulk actions). Contacts export = toolbar `s-menu` (All contacts / Current page) + Export selected in the bar. |
| 2026-08-12 | **Contacts trims** (user request): sort control removed (always newest-first) and header Export button + ContactsExportModal deleted — bulk "Export selected" from row selection is the only export path (action keeps page/all scopes as a seam). |
| 2026-08-12 | **Contacts table consistency** (user request): tabs + sort select moved into DataTable toolbar (sort was a state-toggle button whose label showed the CURRENT mode — read as broken; now an explicit Newest/Name A–Z `s-select`); collapsible search; functional items-per-page via new controlled `perPage`/`onPerPageChange` + controlled `search`/`onSearchChange` DataTable props (lifted because the "current page" CSV export re-computes the visible slice server-side — `exportContactsCsv` gained `pageSize`); bulk **Export selected** (new `scope: "selected"` + ids). |
| 2026-08-12 | **Table-furniture consistency pass** (user: same pattern everywhere): `DataTable` v2 now carries the full discount_screen_2.png furniture — built-in collapsible search icon, `toolbar` slot for pills, `bulkActions` selection (checkbox column + "N selected" bar), always-visible footer (centered pager + items-per-page 10/25/50; `perPageSelector={false}` for Contacts' server-fixed size). Products + Collections gained bulk **Enable/Disable learning** (`products-learn`/`collections-learn` ids intents); Discounts tab refit back onto DataTable (bespoke s-table copy deleted). Known backlog: raw `<input>`s remain in browse modals, FaqManager, SettingsAvailability, ChipInput, RecommendationDetailCustom, SettingsGeneral, TrainingKnowledgeTab (multiline JSX escaped the first sweep). |
| 2026-08-12 | **App-wide native-table sweep** (user directive: use Polaris components wherever one exists): shared `DataTable.tsx` internals refit to the `s-table` family — upgrades Products, Collections, Contacts, Curated answers, Review queue, and Proactive campaigns tables in one move (row clicks now via `clickDelegate` → `s-clickable` on the first cell since `s-table-row` has no onClick); `TrainingKnowledgeTab` sources table + products `ViewTable` converted from raw `<table>`. Deliberately left custom (documented in skill): Inbox 3-pane chat UI, TestAiConsole chat surface, `ui-save-bar` plain buttons (App Bridge contract), `_index` public login page (no App Bridge). |
| 2026-08-12 | **Discounts tab round 2 (`discount_screen_2.png`)**: table refit to native `s-table` family (user directive: modern Polaris components for tables — skill updated, `DataTable.tsx` now legacy); collapsible search icon; row selection + bulk **Enable/Disable for AI** (user decision: app-only `Discount.learnEnabled`, never mutates Shopify status — design's "Set as active/inactive" wording dropped for that reason); per-row AI switch + wired Learn card master switch (`discounts-learn`/`discounts-learn-all` intents); `activeDiscountContext` filters `learnEnabled`; centered pager + one-line items-per-page. Migration `20260812084842_discount_learn_enabled` (hand-trimmed again — same auto-diff index-drop trap; also had to delete the stale rolled-back `_prisma_migrations` row before `migrate dev` would run). |
| 2026-08-12 | **Discounts training tab rebuilt to `resources/other/discount_screen.png`** (user request): full discounts table (All/Active/Inactive pills, search, Title+summary / Status / Method / Type / Start / End / Used, items-per-page pager) + Manage button → store Discounts admin. Migration `20260812064603_discount_display_fields` adds `Discount.method`/`discountType`/`usedCount`; sync GraphQL now pulls `discountClasses` + `asyncUsageCount` (validated 2026-07); discount webhook upsert refetches the single node (payload lacks summary/classes/usage) with payload fallback. ⚠️ prisma migrate dev's auto-diff tried to drop the 5 custom HNSW/GIN indexes + alter generated `searchText` (known drift) — migration was hand-trimmed; always review generated SQL. |
| 2026-08-10 | **Search + OOS-rules batch** (user-reported "black bracelets" miss): ① keyword search now `ORDER BY ts_rank` (was arbitrary LIMIT rows — title matches got crowded out by description matches); ② purchasability = `stock > 0 OR any variant availableForSale` everywhere (SQL `PURCHASABLE` fragment in product-search + `purchasableWhere()` Prisma mirror in pipeline) — fixes untracked-inventory/"continue selling" products (totalInventory=0) being hidden while buyable; webhook sync's variant `available` now mirrors availableForSale (untracked or policy=continue ⇒ true); ③ **"Never recommend out-of-stock" toggle is now FUNCTIONAL — user decision, diverges from spec 08's always-on exclusion**: stored in `shopSettings.recommendationRules.excludeOutOfStock` (default true), saved via instructions `save-rules` intent, threaded through hybrid search, browse-cheapest, custom-rec pool, curated/recommendation/cross-sell card assembly; OFF ⇒ unavailable products may appear in cards (numericVariantId already falls back to first variant). |
| 2026-08-06 | Dev DB = Docker Postgres + pgvector (user choice); replaces SQLite entirely |
| 2026-08-06 | LLM = OpenAI gpt-4o-mini + text-embedding-3-small behind LlmProvider interface (user choice) |
| 2026-08-06 | Orchestration v1 = router-first; tool-calling later (user choice) |
| 2026-08-06 | Streaming = SSE (POST fetch-stream) via app proxy; fallback = signed-token direct streaming |
| 2026-08-06 | Jobs = pg-boss on same Postgres; webhook handlers enqueue-only |
| 2026-08-06 | Scopes v1 = read_products only; read_orders/PCD deliberately deferred |
| 2026-08-06 | No RLS v1 — tenancy via code seam + tenancy-auditor |
| 2026-08-06 | History window: 10 msgs for router / 6 for generation + rolling summary (resolves demo-vs-guide inconsistency) |
| 2026-08-06 | Router parse failure → retry then chat-clarify (fixes demo's default-to-buy bug) |
| 2026-08-06 | **PROVEN: Prisma 6.16 differ does NOT ignore Unsupported-column indexes** — raw `migrate dev` regenerates DROP INDEX for all HNSW/GIN indexes + strips the generated tsvector expression. Mitigation shipped & tested: all migrations via `npm run migrate:new -- --name <x>` (create-only → scrub-migration.ts → apply); smoke test asserts the 5 protected indexes exist. db-tenancy skill + spec 01 updated. |
| 2026-08-06 | Docker Desktop = per-user install; CLI at `%LOCALAPPDATA%\Programs\DockerDesktop\resources\bin` (may need PATH in fresh shells) |
| 2026-08-06 | **Plan gating = ALLOW-ALL mode for now** (user directive): the spec-15 matrix is provisional/dummy. `requirePlan`/`getQuota` seams are wired at every gate point but enforcement mode is "open" — all features shown & usable on every plan, no locks/limits. User will define final per-tier features later; flipping to enforced = one config change in `app/lib/billing/plans.server.ts`. |
| 2026-08-06 | OPENAI_API_KEY verified working — dev shop re-seeded with real embeddings |
| 2026-08-06 | App-recommendation runtime layer added to pipeline (pre-08, orchestrator-built): per-trigger embeddings via in-memory cache (joined-string embedding dilutes matches — eval-proven), threshold = curatedMatchThreshold, ranked below merchant curated (golden 9/9 proves ordering). Prompts unchanged — freeze intact |
| 2026-08-07 | **App proxy subpath changed `chatconvert` → `ccwidget`.** The original subpath's record on the dev store was pinned to the `https://example.com/proxy` placeholder; moving to a fresh subpath guaranteed a clean binding. Changed in 3 places that must stay in sync: `shopify.app.toml` `[app_proxy].subpath`, `chat-widget.liquid` `data-proxy-base`, and the fallback in `assets/chat-widget.js`. Storefront path is now `/apps/ccwidget/*`. |
| 2026-08-07 | **PROVEN: a store pins the app-proxy destination at install time and never refreshes it.** Not `npm run deploy`, not restarting `shopify app dev`, not a scope re-authorization (which refreshes the access token, so it *looks* like it worked). Only a genuine uninstall → fresh install rewrites it. Symptom: storefront 500 + "There was an error in the third-party application", ~99ms, zero requests reaching the tunnel; widget renders nothing because its `widget-config` fetch fails silently by design. **Any `[app_proxy]` change (url/prefix/subpath) requires uninstall+reinstall on every store.** Documented in README. |
| 2026-08-07 | **Dev must run on a self-managed tunnel**, not the CLI's default. `npm run dev` mints a new `trycloudflare.com` hostname per restart; combined with install-time pinning the registered proxy URL goes stale within minutes. Workflow: start `cloudflared tunnel --url http://localhost:3000 --metrics 127.0.0.1:20241` once, then `npm run dev -- --tunnel-url https://<host>:3000`. Survives dev restarts. |
| 2026-08-07 | Relative `[app_proxy].url` (`/proxy`) is documented by Shopify as auto-prefixed with `application_url`, but **could not be made to resolve** in practice — deployed relative URLs gave storefront 500s with no request reaching the tunnel. Use the absolute form. |
| 2026-08-07 | `write_app_proxy` scope added — required to configure `[app_proxy]` at all. Scopes now 6; merchant re-auth required. |
| 2026-08-07 | `PRISMA_CLIENT_ENGINE_TYPE=binary` enabled in `.env` (was commented). Prisma Studio and stale `react-router dev` processes lock `query_engine-windows.dll.node` on Windows, making the CLI's pre-dev `npx prisma generate` fail with `EPERM` — the app server then never boots and the CLI reports `Unreachable target`. Stop Studio before `npm run dev`. |
| 2026-08-10 | **PROVEN: releasing an app version from the Partner dashboard (e.g. manually editing URLs after a tunnel change) publishes config WITHOUT the theme-extension asset bundle.** The embed liquid still renders and the proxy still works, but chat-widget.js/.css 404 on the CDN → widget silently renders nothing. Fix: `npm run deploy` (CLI) to release a version with assets. Rule: after a tunnel change, never edit URLs in the dashboard — sync `shopify.app.toml` and let the CLI handle it (`automatically_update_urls_on_dev` covers dev; deploy covers releases). |
| 2026-08-06 | Handover RUNTIME added to pipeline (pre-10, orchestrator-built): explicit-ask/sentiment/repeat pre-router + intent-rules post-embedding + cannot-answer escalation after fallback turns; executeHandover applies destination config (inbox/collect_email/contact_methods), aiWhileWaiting × availability dormancy, sys messages, handover frame for widget. Explicit-ask path verified live (conversation flagged, mode=human). Golden 9/9 unaffected. Merchant email notify + widget form UI → feature 10 |
| 2026-08-10 | **FAQ manager rebuilt to match `.claude/resources/other/faq.png`** (Chatty-style, user-provided reference). Patterns now standard for list-manager UIs: (1) toolbar = grouped menu buttons via `s-button commandFor` + `s-menu` ("More actions" → Import/Export, primary "Add new" → Add FAQ/Add category) instead of a row of flat buttons; (2) reordering = drag handles (`s-icon type="drag-handle"`, HTML5 DnD, before/after midpoint detection) replacing ↑/↓ Order columns — FAQ tree supports cross-category drops (new `placeFaq`/exported `placeCategory` + `faq-place`/`category-place` intents; drag disabled while filtered/busy; handles keep ArrowUp/Down keyboard fallback via the old move intents); (3) shared `app/components/DragReorder.tsx` (`useDragReorder` + `DragHandle` + `arrayMove`) applied to chatbox starter questions and contact methods (client-side lists, saved via save bar). Campaigns table keeps ▲▼ (paginated/searchable DataTable — drag indexes ambiguous). |
| 2026-08-10 | **Team v1 + discount toggle** (user: "complete logic"): ① **Team roster** in ShopSettings JSON (`team.members[]` — id/name/email/role/since via `teamMemberSchema`; no migration): settings intents `team-invite` (dup-email guard, randomUUID) / `team-role` / `team-remove` (auto-unassigns their conversations); SettingsGeneral rebuilt — working Invite modal (name/email/role + note that login access = Shopify staff accounts), owner row + member rows w/ inline role select + confirm-remove; self-contained teamFetcher w/ toasts. ② **Inbox assignment live**: `assign` intent (roster-validated, ""→null), assignee select in details panel (owner + members), loader ships `assignees`. ③ **Real-time discount sync switch functional**: `discountRealtime` in ShopSettings (default on), Discounts-tab switch persists via `discount-realtime` intent (requirePlan seam), webhook handler honors plan gate AND merchant toggle. |
| 2026-08-10 | **User-feedback batch 3**: analytics paired-card grids now stretch (removed `alignItems:"start"`) so side-by-side cards share equal card-background heights; plan cards grid fixed at 2 columns (2×2 for the four tiers, user decision); **all dev-DB shops set to plan=plus** via new `scripts/set-plan.ts` utility (enforcement stays "open"; matrix still provisional — real tiers decided after full app testing; re-run `npx tsx scripts/set-plan.ts <tier>` anytime). |
| 2026-08-10 | **User-feedback batch 2** (5 items): ① `deleteCategory` (FAQs move to Uncategorized, default undeletable) + `category-delete` intent + Delete in category modal; ② FAQ/category delete confirmations = Shopify-style modal (`ui/ConfirmDeleteModal`, z-500 above BrowseModalShell) replacing the banner pattern; ③ **widget conversation persists across storefront navigation** — open/closed + screen state in sessionStorage (`cc:open`/`cc:screen`), auto-reopen on load, thread history restored via new `proxy.history` route + `getWidgetThreadHistory` (sessionId-bound ownership, in/out roles + productCards, take 100); intro/starters render only for fresh conversations; human-mode polling resumes from stored pollSince; ④ launcher `bgColor` setting (schema `""`=brand, appearance tab checkbox + s-color-field, applied in shell + renderer preview); ⑤ contact-method icons = professional filled glyphs (real WhatsApp mark #25d366, Material phone/mail #454549) at 22px, **no background tiles**, hover wash only. Widget 10.81KB gz (≤30). |
| 2026-08-10 | **Functional refinements** (Stage 4, all 9): ① chatbox activate-while-dirty now blocked w/ toast (silent-data-loss bug); ② **discount grounding phase 1** — `activeDiscountContext` in pipeline question lane: discount-intent regex → inject active in-window Discount rows (title+summary, take 6) into store-info context, no-knowledge fallback bypassed when discount facts exist; zero extra LLM calls, prompts untouched (spec 02:68 delta closed); ③ FAQ import/export full round-trip (header-aware import restores category [created by name]/status/featured; splitCsv exported from sources.server); ④ `{{cart_total}}` merge tag in campaign bubbles (shell fetches /cart.js when tag present; renderer formats or degrades to "your cart"); ⑤ curated served-KPI month-accurate (curated_served events); ⑥ contacts→inbox deep links (`?c={id}`); ⑦ analytics 12m range (365 daily buckets, chart label thinning handles it); ⑧ **widget FAQ server-side search** — new `proxy.faq-search` route (signed-proxy identity, published-only, sanitized at serve) + debounced widget search w/ client-filter fallback; ⑨ chatbox preview now uses shop currency (Chatty/Chittpa copy already clean; plan CTAs + quota meters verified matrix-driven). Gates: typecheck/lint/widget-size (10.09KB gz) green; smoke PASS; **tenancy-auditor run on the full diff: 0 leaks**, its one RISK note (placeCategory guard) fixed same day. |
| 2026-08-10 | **Consistency sweep** (Stage 3): analytics perf tiles + curated/contacts/proactive KPI tiles on ui/StatTile with semantic icon chips/tones; contacts search → s-search-field, sort button icon="sort", dead rows-per-page select removed, tabs → TabPills; proactive "Last 7 days" badge fixed to "All time", template-picker literal ← → arrow-left icon; ai-agent banners → StripBanner, stray button whitespace stripped; TrainingProductsTab dead Collections/FAQs columns removed; settings+chatbox button-tabs → TabPills; chatbox grid gap 20→16; DataTable upgraded (s-search-field, design table shell w/ uppercase muted headers + row hover, chevron pager, `empty` slot); old components/StatTile.tsx deleted. |
| 2026-08-10 | **Dashboard upgraded to reference design** (Stage 2): gradient hero banner (heroGradient card, decorative circles, greeting+👋 moved into hero — s-page heading now "Home", translucent pulsing online pill, white/ghost custom hero buttons w/ focus rings), priority status banner (AI-off warning → quota ≥80% [auto-hidden while enforcement open — getQuota returns UNLIMITED] → dismissible setup-steps info via localStorage), overview tiles rebuilt on ui/StatTile (icon chips, tones, delta pills, day/month-bucketed sparklines via new `conversationSeries`/`atcSeries` raw date_trunc queries in dashboard.server.ts; live tile uses the `live` variant — hand-inlined fork deleted), live-feed EmptyState. |
| 2026-08-10 | **Design-system foundation `app/components/ui/`** (refinement plan Stage 1, user-approved): tokens.ts (SPACE/RADIUS/BRAND/SHADOW/TONES — reference-design values), PageHeader, TabPills (replaces all 5 tab implementations; SubTabs now a thin wrapper), Row/Toolbar (rule: no new raw `gap: N`), upgraded StatTile/StatGrid (icon chip + tones + delta pill + sparkline + live variant + inset/elevated), StripBanner (two-part), EmptyState, Progress (ring extracted from DashboardChecklist + gradient ProgressTrack now used by QuotaMeter). **Brand-gradient policy flipped by user decision**: gradient allowed on hero/marketing/progress surfaces, Polaris-native elsewhere — recorded in polaris-admin-ui skill. All hardcoded `#6d3bf5` admin-chrome sites now use BRAND (widget data colors in schemas/templates/appearance presets intentionally stay literal). |
| 2026-08-10 | **Icons-only + drag-from-handle polish** (user directive): all UI text glyphs (▸▾▲▼★☆✕✓←→↑↓) replaced with official Polaris `s-icon`/`s-button icon` across the app (FAQ chevrons/stars, modal/chip closes → `x`, campaign priority → `caret-up/down`, back buttons → `arrow-left`, StatTile deltas → `arrow-up/down`, inbox + analytics star ratings → `star`/`star-filled`, resolve ✓ → `check`); decorative trailing "→" dropped from buttons/links; merchant-editable emoji (category icons, starters) stay. **Use only supported Polaris icons for all future UI** — rule recorded in polaris-admin-ui skill. Drag now starts ONLY from the handle (rows scroll/select normally — Chatty behavior); FAQ search filtering deferred via useDeferredValue; FAQ filters = `s-clickable-chip` + `s-popover` inline dropdowns; FAQ table = rounded container + row hover shadow (scoped CSS). |
| 2026-08-10 | **App-wide save UX standardized** (user feedback: FAQ save looked frozen). (1) Modal primary actions show `s-button loading` while their fetcher is in flight (FaqManager, TrainingKnowledgeTab, delete banners, AI toggle). (2) Page-level forms use ONLY the App Bridge contextual save bar — removed the duplicate bottom Save/Cancel rows from Instructions tabs and Settings sub-views (SettingsPrivacy/Survey/Availability props slimmed). (3) Full-page editors converted to the SaveBar pattern with JSON-snapshot dirty tracking + back-chevron headers: curated-answers editor, ProactiveCampaignEditor (route owns save; editor lost onSave/saving props), RecommendationDetail, RecommendationDetailCustom. Rule going forward: new admin forms = contextual save bar; modals = footer button with `loading`. |

## Wave-1 gate results (2026-08-06)

- **Tenancy audit: 0 LEAK / 1 RISK — FIXED** (rate-limit bucket now keyed `${shopId}:${sessionId}` + stale-prune instead of global clear). Same-shop notes accepted: conversation ids are non-enumerable cuids; customerRedact email-only + missing no-email RedactLog → owned by feature 17.
- **Shopify review: 2 Critical / 3 Major / 5 minor — ALL FIXED** except accepted notes:
  - C1/C2: scopes now `read_products,read_discounts,write_files` (discounts + Files API were dead code without them) — **merchant re-auth required on next install**
  - M1: daily retention-purge job shipped (Settings promise now enforced); M2: customerRedact scrubs DataRequest emails; M3: cleanupShop deletes Session rows (offline token + owner PII)
  - m1: discount-sync plan-gate seam added; m2: FAQ HTML sanitized at serve time; m3: embed deep link now carries `activateAppId={api_key}/chat-widget`; m4: widget CSS lazy-loads after config check (disabled shops pay zero bytes)
  - m5 accepted: conversationId as bearer capability (cuid unguessability), noted for 10

## Wave-2 gate results (2026-08-06)

- **Tenancy audit: 0 LEAK.** Findings all FIXED: inbox preview raw SQL used snake_case columns (failed closed — would have broken every Inbox load; fixed + live-verified); cleanupShop now deletes crossSellPair + unresolvedQuestion; customerRedact + retentionPurge now delete unresolved-question copies of purged/redacted conversations. Accepted note: proxy conversationId remains cuid-gated (defense-in-depth binding to sessionId in backlog).
- **Shopify review: 1 Critical / 3 Major / 6 minor — FIXED** except accepted/backlogged:
  - C1 FIXED: conversation ownership now bound to widget sessionId across proxy.messages (+widget param), proxy.survey, submitHandoverForm, and the pipeline's by-id conversation resume (v1 cuids are NOT unguessable — id alone was a weak bearer token)
  - M1 was already fixed post-tenancy-audit (same finding); M2 FIXED: customers/redact matches email OR shopifyCustomerId; M3 FIXED: unverified handover submissions can no longer hijack an existing contact's identity (customer match → fresh lead row; no sessionId/phone overwrite)
  - m1 FIXED: review-queue → curated prefill deep link now opens the add form + marks handled on save; m4 FIXED: handover form labels htmlFor + role=alert/aria-live; m6 FIXED: intentRules capped at 20
  - Backlogged (Follow-up backlog): m2 unbounded loader queries (server pagination before scale), m3 collection-filter 250-member truncation, m5 swap regex sanitizer for a vetted dependency

## Blockers / open questions

- SSE-through-app-proxy behavior unknown until probe runs (fallback designed).
- Sales-share KPI + order attribution need orders scope (PCD) — deferred; v1 uses cart-attribute attribution.
- Shopify Files vs app CDN for logo/icon uploads — decide in 06.

## Follow-up backlog (build-discovered)

- PDF/DOCX text extraction for file sources (needs a parser dependency decision; sources currently error cleanly with "parser pending")
- SSRF guard: pinned-socket anti-DNS-rebinding hardening (current: per-hop resolve-then-fetch)
- Served-this-month KPI on curated page → month-accurate via curated_served events (one-line swap)
- proxy.messages: bind conversation lookups to widget sessionId (bearer-capability hardening, with 10)
- Activate-while-dirty on chatbox page discards unsaved edits (UX edge)
- Server pagination for training/contacts/browse-data loaders (unbounded at 10k+ products)
- Collection filter truncates at 250 members (add cursor loop or surface truncation)
- Swap regex HTML sanitizer for sanitize-html/DOMPurify-server (dependency decision, with pdf-parser)

## Design-gap backlog (referenced in designs, not drawn — minimal specs exist)

Unresolved-questions review queue (07) · suggested-Q&A review (04/07) · proactive campaign editor (12) · CSV column-mapping step (04) · AI settings / Automation settings / Translation settings screens · analytics export button placement (14) · team invites (16) · cross-sell pair editor (08).

## Naming / consistency cleanups (from design review)

"Chatty" → ChatConvert (handover copy) · "Chittpa" default widget name → blank→"ChatConvert" · currency = shop currency (designs mix ₹/$) · plan CTAs must say Upgrade/Downgrade/Current correctly · quota meters derive from plan matrix, never hard-coded Plus values.
